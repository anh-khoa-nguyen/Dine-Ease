# Phân Tích Yêu Cầu & Đặc Tả Use Case - Hệ Thống Đặt Bàn Nhà Hàng

## 1. Phân Tích Yêu Cầu (Requirements Analysis)
Hiện nay, việc đặt bàn tại các nhà hàng thường diễn ra thủ công qua điện thoại hoặc đến trực tiếp. Khách hàng gặp khó khăn trong việc tìm kiếm không gian phù hợp, không biết trước thực đơn, giá cả và tình trạng bàn trống. Ngược lại, các nhà hàng cũng chật vật với việc quản lý lịch hẹn bằng sổ sách, khó sắp xếp sơ đồ bàn tối ưu và thường xuyên chịu thiệt hại do tình trạng khách đặt nhưng không đến (no-show). Để giải quyết triệt để vấn đề này, **"Hệ Thống Đặt Bàn Nhà Hàng Trực Tuyến"** được xây dựng nhằm tạo ra một nền tảng (platform) số hóa, làm cầu nối trực tiếp giữa thực khách và các cơ sở kinh doanh dịch vụ F&B.

Hệ thống được thiết kế để mang lại lợi ích toàn diện cho cả 3 nhóm đối tượng tham gia. Đối với **Khách hàng**, nền tảng mang đến trải nghiệm tiện lợi khi cho phép tìm kiếm nhà hàng theo vị trí/loại ẩm thực (cuisine), tham khảo menu, đặt chỗ 24/7 và thanh toán tiền cọc an toàn qua cổng điện tử (VNPay/Momo). Đối với **Nhà hàng**, hệ thống cung cấp bộ công cụ quản trị mạnh mẽ giúp số hóa thực đơn, theo dõi sơ đồ bàn trực quan theo thời gian thực, quản lý đơn hàng và tự động hóa quy trình xác nhận. Cuối cùng, đối với **Ban quản trị (Admin)**, hệ thống cung cấp các công cụ kiểm duyệt đối tác chặt chẽ, cấu hình mức chiết khấu (commission) linh hoạt và hệ thống báo cáo thống kê tổng quan để theo dõi, định hướng sự phát triển của toàn bộ nền tảng.

### 1.1. Đối tượng sử dụng (Actors)
Hệ thống bao gồm 3 phân hệ người dùng chính:
*   **Khách hàng (End User):** Người dùng cuối tìm kiếm nhà hàng, xem menu, đặt bàn và đánh giá.
*   **Nhà hàng (Business User):** Quản lý thông tin gian hàng, thực đơn, sơ đồ bàn và xử lý đơn đặt bàn.
*   **Admin (Quản trị hệ thống):** Quản lý toàn bộ hoạt động của nền tảng, duyệt nhà hàng, cấu hình hệ thống và xem báo cáo.
*   **Actor phụ (Hệ thống ngoài):** Cổng thanh toán (VNPay/Momo), Hệ thống gửi Email, Dịch vụ lưu trữ ảnh (Cloudinary/S3).

---

## 2. Sơ Đồ Use Case Tổng Quát (General Use Case Diagram)
![Sơ đồ Use Case Tổng quát](./screenshots/usecase-general.png)

---

## 3. Đặc Tả Use Case & Wireframes Theo Từng Phân Hệ

### 3.1. Phân hệ Khách hàng (Customer)
#### 3.1.1. Sơ đồ Use Case Khách hàng
![Sơ đồ Use Case Khách hàng](./screenshots/UsecaseKH.png)

#### 3.1.2. Chi tiết Đặc tả & Wireframes
**1. UC_CUSTOMER_01: Tìm kiếm nhà hàng**

| Trường thông tin | Nội dung |
| :--- | :--- |
| **Use case Id** | UC_CUSTOMER_01 |
| **Name**<br>(Tên use case) | Tìm kiếm nhà hàng |
| **Description**<br>(Mô tả) | Use case này cho phép Khách hàng tìm kiếm các nhà hàng trên hệ thống dựa trên từ khóa (tên nhà hàng) hoặc kết hợp với các bộ lọc (vị trí, loại ẩm thực - cuisine, khoảng giá). Hệ thống cũng cung cấp tính năng gợi ý nhà hàng khi khách hàng bắt đầu tìm kiếm. |
| **Primary Actor**<br>(Actor chính) | Khách hàng (End User) |
| **Secondary Actor**<br>(Actor phụ) | Hệ thống Cơ sở dữ liệu (PostgreSQL/MySQL) |
| **Pre-conditions**<br>(Tiền điều kiện) | 1. Khách hàng truy cập vào trang chủ hoặc trang tìm kiếm của ứng dụng.<br>2. *(Không bắt buộc)* Khách hàng có thể đã đăng nhập hoặc chưa đăng nhập. |
| **Post-conditions**<br>(Hậu điều kiện) | **Thành công:**<br>1. Hệ thống hiển thị danh sách các nhà hàng phù hợp với tiêu chí tìm kiếm/lọc của khách hàng.<br><br>**Thất bại:**<br>1. Hệ thống hiển thị thông báo không tìm thấy kết quả hoặc thông báo lỗi hệ thống. |
| **Main flows**<br>(Luồng hoạt động chính) | 1. Use case bắt đầu khi Khách hàng truy cập vào trang chủ hoặc trang tìm kiếm.<br>2. Khách hàng nhập từ khóa (Ví dụ: "Hải sản", "Bún chả") vào thanh tìm kiếm.<br>3. Khách hàng chọn thêm các tiêu chí lọc nếu muốn (Ví dụ: Khu vực: "Quận 1", Cuisine: "Món Việt") .<br>4. Khách hàng nhấn nút **"Tìm kiếm"** (hoặc nhấn Enter/biểu tượng kính lúp).<br>5. Ứng dụng (Frontend) thu thập từ khóa và các tham số lọc, gửi yêu cầu `GET` xuống Backend.<br>6. Backend tiếp nhận yêu cầu, xử lý logic và truy vấn Cơ sở dữ liệu (CSDL) để tìm các nhà hàng khớp với điều kiện.<br>7. Backend trả về danh sách nhà hàng (có phân trang), bao gồm các thông tin tóm tắt: Tên nhà hàng, Hình ảnh đại diện, Địa chỉ, Điểm đánh giá trung bình, và Trạng thái (Đang mở/Đóng cửa).<br>8. Frontend hiển thị danh sách kết quả tìm kiếm cho Khách hàng.<br>9. Khách hàng có thể cuộn để xem, chuyển trang, hoặc nhấn vào một nhà hàng cụ thể để xem chi tiết.<br>10. Use case kết thúc. |
| **Alternative flows**<br>(Luồng thay thế) | **2a. Hiển thị Gợi ý nhà hàng :**<br>1. Tại bước 2, ngay khi Khách hàng click chuột vào thanh tìm kiếm (chưa gõ từ khóa hoặc mới gõ 1-2 ký tự).<br>2. Frontend tự động gọi API lấy danh sách gợi ý (Ví dụ: Các nhà hàng đang hot, Top nhà hàng được đặt nhiều nhất, hoặc Lịch sử tìm kiếm gần đây).<br>3. Hệ thống hiển thị danh sách gợi ý dưới dạng dropdown (trình đơn thả xuống).<br>4. Khách hàng click chọn trực tiếp một nhà hàng trong danh sách gợi ý.<br>5. Hệ thống chuyển hướng Khách hàng thẳng đến trang "Xem chi tiết nhà hàng" đó.<br>6. Use case kết thúc sớm.<br><br>**7a. Không tìm thấy kết quả phù hợp:**<br>1. Tại bước 6, Backend truy vấn CSDL nhưng không có nhà hàng nào khớp với từ khóa và bộ lọc của Khách hàng.<br>2. Backend trả về một mảng dữ liệu rỗng.<br>3. Frontend hiển thị thông báo: *"Rất tiếc, không tìm thấy nhà hàng nào phù hợp với tìm kiếm của bạn."*<br>4. Hệ thống tự động hiển thị thêm một danh sách *"Gợi ý các nhà hàng khác bạn có thể thích"* để giữ chân khách hàng.<br>5. Khách hàng thay đổi từ khóa/bộ lọc và tìm kiếm lại (Quay lại Bước 2).<br><br>**2b. Tìm kiếm trống (Chỉ sử dụng bộ lọc):**<br>1. Tại bước 2, Khách hàng để trống thanh tìm kiếm từ khóa.<br>2. Tại bước 3, Khách hàng chỉ chọn bộ lọc (Ví dụ: Chỉ chọn Cuisine "Món Nhật").<br>3. Hệ thống vẫn xử lý bình thường, Backend sẽ bỏ qua điều kiện từ khóa và trả về toàn bộ danh sách nhà hàng "Món Nhật".<br>4. Luồng hoạt động tiếp tục tại Bước 7. |
| **Exception flows**<br>(Luồng ngoại lệ) | **5a. Lỗi dữ liệu đầu vào:**<br>1. Tại bước 5, Khách hàng (hoặc bot) cố tình can thiệp vào URL, truyền lên các tham số tìm kiếm độc hại (Ví dụ: chứa mã SQL Injection, mã XSS, hoặc tham số phân trang âm ).<br>2. Backend (thông qua Spring Validation) phát hiện dữ liệu không hợp lệ hoặc chứa ký tự nguy hiểm.<br>3. Backend chặn truy vấn ngay lập tức và trả về mã lỗi `400 Bad Request`.<br>4. Frontend hiển thị thông báo: *"Dữ liệu tìm kiếm không hợp lệ. Vui lòng thử lại."*<br>5. Use case kết thúc thất bại.<br><br>**6a. Lỗi kết nối Cơ sở dữ liệu (Database Timeout/Down):**<br>1. Tại bước 6, Backend không thể kết nối tới PostgreSQL/MySQL hoặc câu lệnh truy vấn tìm kiếm chạy quá lâu gây Timeout (do hệ thống đang quá tải).<br>2. Backend bắt lỗi và trả về mã `500 Internal Server Error` hoặc `504 Gateway Timeout`.<br>3. Frontend hiển thị thông báo lỗi: *"Hệ thống đang bận hoặc mất kết nối. Vui lòng thử lại sau ít phút."*<br>4. Use case kết thúc thất bại. |

---

*Wireframe tương ứng:*

![Wireframe Trang chủ](./screenshots/wireframes/customer/customer-01-home.png)

![Wireframe Tìm kiếm nhà hàng](./screenshots/wireframes/customer/customer-02-search-results.png)

**2. UC_CUSTOMER_02: Xem menu và giá**
| Trường thông tin | Nội dung |
| :--- | :--- |
| **Use case Id** | UC_CUSTOMER_02 |
| **Name**<br>(Tên use case) | Xem menu và giá |
| **Description**<br>(Mô tả) | Use case này cho phép Khách hàng xem danh sách các món ăn, đồ uống kèm theo giá tiền của một nhà hàng cụ thể. Khách hàng có thể lướt xem theo từng danh mục (Khai vị, Món chính...), xem chi tiết hình ảnh món ăn và tham khảo các món bán chạy nhất (Bestseller) để dễ dàng đưa ra quyết định trước khi đặt bàn. |
| **Primary Actor**<br>(Actor chính) | Khách hàng (End User) |
| **Secondary Actor**<br>(Actor phụ) | Hệ thống Cơ sở dữ liệu (PostgreSQL/MySQL)<br>Dịch vụ lưu trữ hình ảnh (Cloudinary / AWS S3) |
| **Pre-conditions**<br>(Tiền điều kiện) | 1. Khách hàng đang ở trang "Chi tiết nhà hàng" (sau khi đã click chọn một nhà hàng từ kết quả tìm kiếm hoặc trang chủ). |
| **Post-conditions**<br>(Hậu điều kiện) | **Thành công:**<br>1. Hệ thống hiển thị đầy đủ danh sách món ăn, giá cả, phân loại và hình ảnh (nếu có).<br><br>**Thất bại:**<br>1. Hệ thống hiển thị thông báo lỗi không thể tải được thực đơn. |
| **Main flows**<br>(Luồng hoạt động chính) | 1. Use case bắt đầu khi Khách hàng cuộn xuống phần "Thực đơn" (Menu) trên trang Chi tiết nhà hàng, hoặc nhấn vào tab "Menu".<br>2. Ứng dụng (Frontend) gửi yêu cầu kèm theo `restaurant_id` xuống Backend để lấy dữ liệu thực đơn.<br>3. Backend truy vấn CSDL, lấy danh sách các món ăn đang ở trạng thái "Đang bán" (Available), bao gồm: Tên món, Mô tả ngắn, Giá tiền, Phân loại danh mục, và URL hình ảnh thu nhỏ.<br>4. Backend trả dữ liệu về cho Frontend dưới dạng JSON.<br>5. Frontend phân nhóm dữ liệu và hiển thị danh sách các món ăn theo từng danh mục (Ví dụ: Menu Đồ uống, Menu Món chính).<br>6. Khách hàng lướt xem tên món, đọc mô tả thành phần và xem giá tiền.<br>7. Use case kết thúc (Khách hàng có đủ thông tin để chuyển sang bước Đặt bàn online). |
| **Alternative flows**<br>(Luồng thay thế) | **6a. Khách hàng xem hình ảnh chi tiết món ăn:**<br>1. Tại bước 6, Khách hàng muốn xem kỹ hơn về một món ăn cụ thể nên click vào tên món hoặc ảnh thu nhỏ (thumbnail) của món đó.<br>2. Hệ thống hiển thị một Popup/Modal (hộp thoại nổi) phóng to hình ảnh sắc nét của món ăn.<br>3. Popup hiển thị thêm các thông tin chi tiết (nếu có) như: Định lượng (Size M/L), Lượng calo, hoặc các tùy chọn thêm (Topping).<br>4. Khách hàng nhấn nút "Đóng" (X) để tắt Popup.<br>5. Luồng hoạt động quay lại Bước 6.<br><br>**5a. Khách hàng xem các món bán chạy :**<br>1. Tại bước 5, hệ thống tự động trích xuất các món ăn có cờ `is_bestseller = true` và gom chúng vào một danh mục nổi bật nằm trên cùng của Menu (Tên danh mục: "Món Bán Chạy" hoặc "Chef's Recommend").<br>2. Hoặc Khách hàng chủ động click vào tab/bộ lọc "Bestseller".<br>3. Frontend chỉ hiển thị các món ăn được đánh dấu là bán chạy nhất kèm theo nhãn dán (badge) nổi bật (Ví dụ: Icon vương miện hoặc "Must try").<br>4. Khách hàng xem danh sách này để tham khảo.<br>5. Luồng hoạt động tiếp tục tại Bước 6.<br><br>**3a. Nhà hàng chưa cập nhật thực đơn:**<br>1. Tại bước 3, Backend truy vấn nhưng nhà hàng này chưa thêm bất kỳ món ăn nào vào CSDL.<br>2. Backend trả về một mảng dữ liệu rỗng.<br>3. Frontend hiển thị thông báo thân thiện: *"Nhà hàng này hiện chưa cập nhật thực đơn trực tuyến. Vui lòng liên hệ trực tiếp nhà hàng để biết thêm chi tiết."*<br>4. Use case kết thúc. |
| **Exception flows**<br>(Luồng ngoại lệ) | **2a (Luồng 6a). Lỗi tải hình ảnh món ăn:**<br>1. Tại bước 2 của luồng 6a, URL hình ảnh lưu trong CSDL bị hỏng, hoặc dịch vụ lưu trữ ảnh (Cloudinary) đang bị lỗi/sập.<br>2. Frontend không thể tải được hình ảnh thực tế của món ăn.<br>3. Frontend tự động bắt lỗi (onError event của thẻ Image) và thay thế bằng một **"Ảnh mặc định" (Placeholder Image - ví dụ: logo nhà hàng hoặc icon đĩa thức ăn)**.<br>4. Tên món và giá tiền vẫn hiển thị bình thường để không làm gián đoạn trải nghiệm đọc menu của khách.<br><br>**3b. Lỗi mất kết nối Cơ sở dữ liệu (Database Error):**<br>1. Tại bước 3, Backend không thể kết nối tới CSDL để truy vấn menu do CSDL bị quá tải hoặc sập mạng.<br>2. Backend bắt lỗi (catch exception) và trả về mã `500 Internal Server Error`.<br>3. Frontend hiển thị thông báo lỗi: *"Không thể tải thực đơn lúc này. Vui lòng kiểm tra kết nối mạng hoặc thử lại sau ít phút."* kèm theo một nút **"Tải lại" (Retry)**.<br>4. Use case kết thúc thất bại. |

---

*Wireframe tương ứng:*
![Wireframe Xem menu](./screenshots/wireframes/customer/customer-03-restaurant-detail.png)

**3. UC_CUSTOMER_03: Đánh giá nhà hàng**
| Trường thông tin | Nội dung |
| :--- | :--- |
| **Use case Id** | UC_CUSTOMER_03 |
| **Name**<br>(Tên use case) | Đánh giá nhà hàng |
| **Description**<br>(Mô tả) | Use case này cho phép Khách hàng để lại nhận xét (bằng chữ) và chấm điểm (số sao) cho một nhà hàng sau khi họ đã hoàn thành việc đặt bàn và dùng bữa tại đó. Khách hàng cũng có thể đính kèm hình ảnh thực tế để minh họa cho đánh giá của mình. |
| **Primary Actor**<br>(Actor chính) | Khách hàng (End User) |
| **Secondary Actor**<br>(Actor phụ) | Hệ thống Cơ sở dữ liệu (PostgreSQL/MySQL)<br>Cloudinary (Dịch vụ lưu trữ hình ảnh) |
| **Pre-conditions**<br>(Tiền điều kiện) | 1. Khách hàng đã đăng nhập vào hệ thống.<br>2. Khách hàng có ít nhất một đơn đặt bàn tại nhà hàng đó ở trạng thái **"Đã hoàn thành" (Completed)**.<br>3. Khách hàng chưa từng đánh giá cho mã đơn đặt bàn (Reservation ID) này trước đó. |
| **Post-conditions**<br>(Hậu điều kiện) | **Thành công:**<br>1. Bản ghi đánh giá (Review) được lưu vào CSDL.<br>2. Hình ảnh (nếu có) được tải lên Cloudinary và lưu URL vào CSDL.<br>3. Điểm đánh giá trung bình (Rating Score) của nhà hàng được hệ thống tính toán và cập nhật lại.<br><br>**Thất bại:**<br>1. Không có đánh giá nào được lưu.<br>2. Hệ thống hiển thị thông báo lỗi cụ thể. |
| **Main flows**<br>(Luồng hoạt động chính) | 1. Use case bắt đầu khi Khách hàng truy cập vào mục "Lịch sử đặt bàn" hoặc nhận được thông báo nhắc nhở đánh giá từ hệ thống.<br>2. Khách hàng nhấn nút **"Đánh giá"** tại một đơn đặt bàn đã hoàn thành.<br>3. Hệ thống hiển thị Form đánh giá bao gồm: Chọn số sao (từ 1 đến 5 sao), Ô nhập nội dung nhận xét, và Nút đính kèm ảnh.<br>4. Khách hàng chọn số sao (Ví dụ: 5 sao) và nhập nội dung nhận xét (Ví dụ: "Đồ ăn rất ngon, phục vụ nhiệt tình").<br>5. Khách hàng nhấn nút **"Gửi đánh giá"**.<br>6. Hệ thống (Frontend) kiểm tra tính hợp lệ: Số sao là bắt buộc (không được để trống).<br>7. Ứng dụng gửi yêu cầu chứa dữ liệu đánh giá và `reservation_id` xuống Backend.<br>8. Backend kiểm tra lại điều kiện (đơn đặt bàn đã hoàn thành và chưa bị đánh giá trùng lặp).<br>9. Backend lưu bản ghi Review vào CSDL và kích hoạt trigger tính lại điểm trung bình của nhà hàng.<br>10. Backend trả về phản hồi thành công.<br>11. Hệ thống hiển thị thông báo: *"Cảm ơn bạn đã gửi đánh giá!"* và ẩn nút "Đánh giá" của đơn đặt bàn đó.<br>12. Use case kết thúc. |
| **Alternative flows**<br>(Luồng thay thế) | **4a. Khách hàng tải lên hình ảnh :**<br>1. Tại bước 4, Khách hàng nhấn nút **"Đính kèm ảnh"**.<br>2. Khách hàng chọn một hoặc nhiều bức ảnh từ thiết bị (điện thoại/máy tính).<br>3. Frontend hiển thị bản xem trước (preview) của các bức ảnh vừa chọn. Khách hàng có thể nhấn nút (X) để xóa ảnh nếu chọn nhầm.<br>4. Tại bước 7, Frontend đóng gói dữ liệu chữ kèm theo các file ảnh gửi lên Backend.<br>5. Backend nhận yêu cầu, gọi API của **Actor Cloudinary** để tải các file ảnh này lên đám mây.<br>6. Cloudinary xử lý, lưu trữ và trả về các đường dẫn URL an toàn của hình ảnh.<br>7. Backend lưu bản ghi Review cùng với danh sách URL hình ảnh vào CSDL.<br>8. Luồng hoạt động tiếp tục tại Bước 9.<br><br>**2a. Khách hàng cố tình đánh giá đơn hàng chưa hoàn thành:**<br>1. Tại bước 2, nếu Khách hàng bằng cách nào đó (sửa URL) truy cập vào link đánh giá của một đơn đặt bàn đang ở trạng thái "Chờ xác nhận" hoặc "Đã hủy".<br>2. Backend kiểm tra trạng thái đơn hàng và phát hiện vi phạm luồng nghiệp vụ.<br>3. Backend trả về mã lỗi `403 Forbidden`.<br>4. Frontend chuyển hướng khách hàng về trang Lịch sử và hiển thị thông báo: *"Bạn chỉ có thể đánh giá nhà hàng sau khi đã dùng bữa thành công."*<br>5. Use case kết thúc. |
| **Exception flows**<br>(Luồng ngoại lệ) | **5a (Luồng 4a). Lỗi dịch vụ Cloudinary:**<br>1. Tại bước 5 của luồng 4a, khi Backend gọi API của Cloudinary để tải ảnh lên, dịch vụ Cloudinary bị sập hoặc mạng chập chờn dẫn đến Timeout.<br>2. Backend bắt lỗi (catch exception), hủy bỏ toàn bộ giao dịch lưu đánh giá (Rollback) để tránh việc có chữ mà mất ảnh.<br>3. Backend trả về mã lỗi `502 Bad Gateway` hoặc `500 Internal Server Error`.<br>4. Frontend hiển thị thông báo lỗi: *"Hệ thống tải ảnh đang gặp sự cố. Vui lòng thử lại sau ít phút hoặc gửi đánh giá không kèm ảnh."*<br>5. Khách hàng có thể quyết định xóa ảnh đi và nhấn gửi lại.<br><br>**7a. Lỗi dữ liệu độc hại (XSS / Spam Injection):**<br>1. Tại bước 7, Khách hàng (hoặc Bot) cố tình chèn các đoạn mã script độc hại (`<script>alert('hacked')</script>`) vào ô nội dung nhận xét.<br>2. Backend (thông qua bộ lọc Sanitization) phát hiện nội dung chứa ký tự nguy hiểm không kiểm soát được.<br>3. Backend chặn việc lưu CSDL và trả về mã `400 Bad Request`.<br>4. Frontend hiển thị cảnh báo: *"Nội dung đánh giá chứa ký tự không hợp lệ. Vui lòng kiểm tra lại."*<br>5. Use case kết thúc thất bại.<br><br>**9a. Lỗi mất kết nối Cơ sở dữ liệu (Database Error):**<br>1. Tại bước 9, Backend không thể lưu bản ghi vào PostgreSQL/MySQL do CSDL bị sập.<br>2. Backend trả về mã `500 Internal Server Error`.<br>3. Frontend hiển thị thông báo: *"Hệ thống đang bận, không thể gửi đánh giá lúc này."* |

---

*Wireframe tương ứng:*
![Wireframe Đánh giá](./screenshots/wireframes/customer/customer-09-review-form.png)

**4. UC_CUSTOMER_04: Xem lịch sử đặt bàn**
| Trường thông tin | Nội dung |
| :--- | :--- |
| **Use case Id** | UC_CUSTOMER_04 |
| **Name**<br>(Tên use case) | Xem lịch sử đặt bàn |
| **Description**<br>(Mô tả) | Use case này cho phép Khách hàng xem lại toàn bộ danh sách các đơn đặt bàn của mình (bao gồm cả quá khứ và sắp tới). Khách hàng bắt buộc phải xem được chi tiết trạng thái của từng đơn. Tùy thuộc vào trạng thái đó, khách hàng có thể thực hiện các hành động mở rộng như Hủy lịch (nếu chưa ăn) hoặc Đặt lại (nếu đã ăn/đã hủy). |
| **Primary Actor**<br>(Actor chính) | Khách hàng (End User) |
| **Secondary Actor**<br>(Actor phụ) | Hệ thống Cơ sở dữ liệu (PostgreSQL/MySQL) |
| **Pre-conditions**<br>(Tiền điều kiện) | 1. Khách hàng đã đăng nhập thành công vào hệ thống (App/Web). |
| **Post-conditions**<br>(Hậu điều kiện) | **Thành công:**<br>1. Khách hàng xem được danh sách đặt bàn và trạng thái chính xác của chúng.<br>2. Các thao tác mở rộng (Hủy/Đặt lại) được thực thi thành công nếu khách hàng yêu cầu.<br><br>**Thất bại:**<br>1. Hệ thống không tải được dữ liệu và hiển thị thông báo lỗi. |
| **Main flows**<br>(Luồng hoạt động chính) | 1. Use case bắt đầu khi Khách hàng nhấn vào mục "Lịch sử" hoặc "Đơn đặt bàn của tôi" trên thanh điều hướng.<br>2. Ứng dụng (Frontend) gửi yêu cầu kèm theo Token xác thực của Khách hàng xuống Backend.<br>3. Backend xác thực Token, trích xuất `user_id` và truy vấn CSDL để lấy toàn bộ danh sách đặt bàn của user này, sắp xếp theo thời gian (mới nhất xếp trước).<br>4. Backend trả dữ liệu về cho Frontend dưới dạng JSON.<br>5. Frontend nhận dữ liệu và hiển thị danh sách các đơn đặt bàn.<br>6. : Tại mỗi thẻ (card) đơn đặt bàn, hệ thống bắt buộc hiển thị rõ ràng **Trạng thái hiện tại** (Ví dụ:  Chờ xác nhận,  Đã xác nhận,  Đã hoàn thành, Đã hủy) kèm theo các thông tin chi tiết: Tên nhà hàng, Ngày giờ đến, Số lượng khách, Tiền cọc.<br>7. Khách hàng có thể cuộn để xem toàn bộ lịch sử của mình.<br>8. Use case kết thúc. |
| **Alternative flows**<br>(Luồng thay thế) | **6a. Khách hàng muốn hủy lịch:**<br>1. Tại bước 6, đối với các đơn đang ở trạng thái "Chờ xác nhận" hoặc "Đã xác nhận", hệ thống hiển thị thêm nút **"Hủy đặt bàn"**.<br>2. Khách hàng nhấn nút "Hủy đặt bàn".<br>3. Hệ thống hiển thị Dialog xác nhận: *"Bạn có chắc chắn muốn hủy lịch hẹn này? (Tiền cọc có thể không được hoàn lại tùy theo chính sách nhà hàng)"*.<br>4. Khách hàng nhấn "Đồng ý".<br>5. Backend cập nhật trạng thái đơn thành `CANCELLED` và giải phóng bàn cho nhà hàng.<br>6. Hệ thống tải lại danh sách (Quay lại Bước 2).<br><br>**6b. Khách hàng muốn đặt lại:**<br>1. Tại bước 6, đối với các đơn ở trạng thái "Đã hoàn thành" hoặc "Đã hủy", hệ thống hiển thị thêm nút **"Đặt lại" (Rebook)**.<br>2. Khách hàng nhấn nút "Đặt lại".<br>3. Hệ thống trích xuất `restaurant_id` từ đơn cũ và tự động chuyển hướng khách hàng sang màn hình "Đặt bàn online" của chính nhà hàng đó.<br>4. Use case kết thúc (chuyển sang luồng Đặt bàn).<br><br>**3a. Lịch sử trống (Khách hàng chưa từng đặt bàn):**<br>1. Tại bước 3, Backend truy vấn nhưng Khách hàng này chưa có bất kỳ đơn đặt bàn nào trong CSDL.<br>2. Backend trả về mảng rỗng `[]`.<br>3. Frontend hiển thị giao diện trạng thái trống (Empty State) với thông báo: *"Bạn chưa có lịch sử đặt bàn nào."* kèm theo một nút **"Khám phá nhà hàng ngay"**.<br>4. Khách hàng nhấn nút và được chuyển hướng về Trang chủ để tìm kiếm.<br>5. Use case kết thúc. |
| **Exception flows**<br>(Luồng ngoại lệ) | **5a (Luồng 6a). Cố tình hủy đơn không hợp lệ (Bypass UI / API Hacking):**<br>1. Tại bước 5 của luồng 6a, Khách hàng (hoặc bot) dùng công cụ can thiệp API để gửi lệnh "Hủy" cho một đơn đặt bàn **đã quá giờ cho phép hủy** (Ví dụ: sát giờ ăn) hoặc đơn **đã Hoàn thành**.<br>2. Backend kiểm tra logic nghiệp vụ (Validation) và phát hiện hành vi không hợp lệ.<br>3. Backend chặn thao tác cập nhật CSDL và trả về mã lỗi `403 Forbidden` hoặc `400 Bad Request`.<br>4. Frontend hiển thị thông báo lỗi: *"Thao tác không hợp lệ. Đơn đặt bàn này không thể hủy vào lúc này."*<br>5. Use case kết thúc thất bại.<br><br>**3b. Lỗi mất kết nối Cơ sở dữ liệu (Database Error):**<br>1. Tại bước 3, Backend không thể kết nối tới PostgreSQL/MySQL do máy chủ CSDL bị sập hoặc quá tải.<br>2. Backend bắt lỗi (catch exception) và trả về mã `500 Internal Server Error`.<br>3. Frontend hiển thị thông báo lỗi: *"Hệ thống đang bận, không thể tải lịch sử đặt bàn. Vui lòng vuốt xuống để thử lại."*<br>4. Use case kết thúc thất bại. |

---

*Wireframe tương ứng:*
![Wireframe Lịch sử](./screenshots/wireframes/customer/customer-08-booking-history.png)

**5. UC_CUSTOMER_05: Đặt bàn online**
| Trường thông tin | Nội dung |
| :--- | :--- |
| **Use case Id** | UC_CUSTOMER_05 |
| **Name**<br>(Tên use case) | Đặt bàn online |
| **Description**<br>(Mô tả) | Use case này cho phép Khách hàng thực hiện quy trình đặt chỗ trước tại một nhà hàng. Khách hàng chọn ngày, giờ, số lượng người. Hệ thống bắt buộc phải kiểm tra tình trạng bàn trống. Khách hàng có thể tùy chọn vị trí ngồi, để lại ghi chú, và thực hiện thanh toán tiền cọc (nếu nhà hàng yêu cầu). Cuối cùng, hệ thống sẽ gửi thông báo xác nhận. |
| **Primary Actor**<br>(Actor chính) | Khách hàng (End User) |
| **Secondary Actor**<br>(Actor phụ) | Hệ thống Cơ sở dữ liệu (PostgreSQL/MySQL)<br>Cổng thanh toán (Momo)<br>Hệ thống gửi Email (EmailSystem) |
| **Pre-conditions**<br>(Tiền điều kiện) | 1. Khách hàng đang ở trang "Chi tiết nhà hàng".<br>2. Khách hàng đã đăng nhập vào hệ thống (để xác danh tính và liên hệ). |
| **Post-conditions**<br>(Hậu điều kiện) | **Thành công:**<br>1. Bản ghi Đặt bàn (Reservation) được tạo trong CSDL với trạng thái `PENDING` (Chờ xác nhận) hoặc `CONFIRMED` (Đã xác nhận nếu có cọc).<br>2. Email thông báo được gửi đến Khách hàng.<br><br>**Thất bại:**<br>1. Không có bản ghi nào được tạo.<br>2. Hệ thống hiển thị thông báo lỗi (Hết bàn, lỗi thanh toán...). |
| **Main flows**<br>(Luồng hoạt động chính) | 1. Use case bắt đầu khi Khách hàng nhấn nút **"Đặt bàn ngay"** tại trang chi tiết nhà hàng.<br>2. Hệ thống hiển thị Form đặt bàn yêu cầu nhập: Ngày đến, Giờ đến, Số lượng người lớn/trẻ em.<br>3. Khách hàng điền thông tin và nhấn "Tiếp tục".<br>4.: Ứng dụng gửi yêu cầu xuống Backend. Backend truy vấn CSDL để đối chiếu số lượng bàn trống của nhà hàng tại khung giờ đó với số lượng khách.<br>5. Backend xác nhận còn bàn trống và trả về kết quả hợp lệ.<br>6. Hệ thống hiển thị màn hình Điền thông tin liên hệ (Tên, SĐT - tự động điền từ profile).<br>7. Khách hàng kiểm tra thông tin và nhấn **"Xác nhận đặt bàn"**.<br>8. Backend tạo bản ghi Reservation mới trong CSDL với trạng thái `PENDING`.<br>9: Backend gọi **Actor EmailSystem** để gửi một email "Yêu cầu đặt bàn đã được ghi nhận" đến Khách hàng.<br>10. Hệ thống hiển thị màn hình "Đặt bàn thành công" kèm mã đơn hàng.<br>11. Use case kết thúc. |
| **Alternative flows**<br>(Luồng thay thế ) | **4a. Hết bàn trống (Xử lý khi Kiểm tra khả dụng thất bại:**<br>1. Tại bước 4, Backend phát hiện khung giờ Khách hàng chọn đã kín bàn.<br>2. Backend trả về danh sách các khung giờ trống gần nhất (Ví dụ: Trễ hơn 30 phút hoặc sớm hơn 1 tiếng).<br>3. Frontend hiển thị thông báo: *"Khung giờ này đã hết bàn. Vui lòng chọn các khung giờ gợi ý dưới đây."*<br>4. Khách hàng chọn một khung giờ gợi ý mới.<br>5. Luồng hoạt động tiếp tục tại Bước 5.<br><br>**6a. Khách hàng thêm yêu cầu đặc biệt:**<br>1. Tại bước 6, Khách hàng nhấn vào mục "Yêu cầu thêm".<br>2. Khách hàng chọn Vị trí mong muốn từ dropdown (Ví dụ: Cạnh cửa sổ, Phòng VIP, Ngoài trời).<br>3. Khách hàng nhập Ghi chú (Ví dụ: "Setup bàn tiệc sinh nhật, có người dị ứng đậu phộng").<br>4. Tại bước 8, Backend lưu các thông tin vị trí và ghi chú này kèm theo bản ghi Reservation.<br>5. Luồng hoạt động tiếp tục tại Bước 9.<br><br>**7a. Nhà hàng yêu cầu đặt cọc:**<br>1. Tại bước 7, hệ thống nhận diện nhà hàng này có cấu hình "Bắt buộc đặt cọc" (Ví dụ: 200.000 VNĐ).<br>2. Khách hàng nhấn "Thanh toán & Đặt bàn".<br>3. Hệ thống tạo bản ghi Reservation với trạng thái `AWAITING_PAYMENT` (Chờ thanh toán).<br>4. Hệ thống chuyển hướng Khách hàng sang cổng thanh toán của **Actor Momo**.<br>5. Khách hàng thực hiện quét mã QR/thanh toán trên Momo thành công.<br>6. Momo gửi IPN (Webhook) về Backend. Backend cập nhật trạng thái đơn thành `CONFIRMED` (Đã xác nhận).<br>7. Luồng hoạt động tiếp tục tại Bước 9. |
| **Exception flows**<br>(Luồng ngoại lệ) | **4b. Xung đột đặt bàn đồng thời:**<br>1. Tại bước 4, hệ thống báo còn 1 bàn trống. Tuy nhiên, tại bước 8, khi Khách hàng A nhấn "Xác nhận", một Khách hàng B khác đã nhanh tay bấm xác nhận trước đó vài mili-giây và lấy mất bàn cuối cùng.<br>2. Backend (thông qua cơ chế Database Locking / Transaction) phát hiện xung đột và từ chối lưu bản ghi của Khách hàng A.<br>3. Backend trả về mã lỗi `409 Conflict`.<br>4. Frontend hiển thị thông báo: *"Rất tiếc, bàn cuối cùng vừa được người khác đặt. Vui lòng chọn lại khung giờ."*<br>5. Use case kết thúc thất bại.<br><br>**7b (Luồng 7a). Lỗi Cổng thanh toán Momo (Payment Gateway Timeout):**<br>1. Tại bước 4 của luồng 7a, hệ thống Momo bị sập hoặc mất kết nối mạng.<br>2. Khách hàng không thể thanh toán, hoặc thanh toán bị treo.<br>3. Sau 15 phút không nhận được IPN từ Momo, một tác vụ nền (Background Job) của Backend tự động hủy đơn đặt bàn.<br>4. Backend cập nhật trạng thái thành `CANCELLED` và gọi EmailSystem báo cho khách hàng: *"Đơn đặt bàn bị hủy do quá hạn thanh toán."*<br><br>**8a. Lỗi dữ liệu độc hại (Bypass Validation):**<br>1. Tại bước 8, Khách hàng (hoặc Bot) dùng tool can thiệp API, gửi số lượng người là số âm (`-5` người) hoặc ngày đặt bàn trong quá khứ.<br>2. Backend phát hiện dữ liệu phi logic.<br>3. Backend chặn lưu CSDL, trả về lỗi `400 Bad Request`.<br>4. Frontend hiển thị: *"Dữ liệu không hợp lệ. Vui lòng thử lại."* |

---

*Wireframe tương ứng:*
![Wireframe Đặt bàn (ngày)](./screenshots/wireframes/customer/customer-04-booking-step1-date.png)

![Wireframe Đặt bàn (thời gian)](./screenshots/wireframes/customer/customer-04-booking-step1-time.png)

![Wireframe Đặt bàn (số người)](./screenshots/wireframes/customer/customer-04-booking-step1-people.png)

![Wireframe Đặt bàn - Bước điền thông tin](./screenshots/wireframes/customer/customer-05-booking-step2-contact.png)


**6. UC_CUSTOMER_06: Thanh toán đặt cọc**
| Trường thông tin | Nội dung |
| :--- | :--- |
| **Use case Id** | UC_CUSTOMER_06 |
| **Name**<br>(Tên use case) | Thanh toán đặt cọc |
| **Description**<br>(Mô tả) | Use case này mô tả quy trình Khách hàng thanh toán một khoản tiền cọc để giữ chỗ cho đơn đặt bàn của mình. Khách hàng có thể áp dụng mã giảm giá (Voucher) để giảm số tiền cần thanh toán. Hệ thống bắt buộc phải chuyển hướng khách hàng sang cổng thanh toán điện tử (Momo) để xử lý giao dịch an toàn. |
| **Primary Actor**<br>(Actor chính) | Khách hàng (End User) |
| **Secondary Actor**<br>(Actor phụ) | Hệ thống Cơ sở dữ liệu (PostgreSQL/MySQL)<br>Cổng thanh toán (Momo) |
| **Pre-conditions**<br>(Tiền điều kiện) | 1. Khách hàng đang trong luồng "Đặt bàn online" và đã xác nhận thông tin đặt bàn.<br>2. Nhà hàng được chọn có thiết lập chính sách "Bắt buộc đặt cọc" (Ví dụ: Cọc 200k/bàn hoặc cọc 20% tổng bill dự kiến).<br>3. Đơn đặt bàn (Reservation) đã được tạo tạm trong CSDL với trạng thái `AWAITING_PAYMENT` (Chờ thanh toán). |
| **Post-conditions**<br>(Hậu điều kiện) | **Thành công:**<br>1. Giao dịch thanh toán được ghi nhận thành công trong CSDL.<br>2. Trạng thái đơn đặt bàn chuyển sang `CONFIRMED` (Đã xác nhận).<br><br>**Thất bại:**<br>1. Giao dịch không thành công.<br>2. Đơn đặt bàn giữ nguyên trạng thái chờ thanh toán hoặc bị hệ thống tự động hủy (`CANCELLED`) nếu quá hạn. |
| **Main flows**<br>(Luồng hoạt động chính) | 1. Use case bắt đầu khi Khách hàng nhấn "Xác nhận & Thanh toán" ở bước cuối của luồng Đặt bàn.<br>2. Hệ thống hiển thị màn hình Thanh toán, tóm tắt: Thông tin đơn hàng, Số tiền cọc cần thanh toán, và Phương thức thanh toán (Mặc định chọn Momo).<br>3. Khách hàng kiểm tra thông tin và nhấn nút **"Thanh toán ngay"**.<br>4: Backend tạo một mã giao dịch nội bộ (Transaction ID) và gửi yêu cầu tạo URL thanh toán sang API của **Actor Momo**.<br>5. Momo trả về một Payment URL. Frontend chuyển hướng (redirect) Khách hàng sang trang thanh toán an toàn của Momo (hoặc mở app Momo qua deep link).<br>6. Khách hàng thực hiện quét mã QR hoặc xác nhận thanh toán trên ứng dụng Momo thành công.<br>7. Momo hiển thị thông báo thành công và tự động chuyển hướng Khách hàng quay lại màn hình "Kết quả giao dịch" của ứng dụng nhà hàng.<br>8. Đồng thời (chạy ngầm), Momo gửi một IPN (Webhook) về Backend để báo cáo tiền đã vào tài khoản nhà hàng/nền tảng.<br>9. Backend đối chiếu chữ ký bảo mật (Signature), cập nhật trạng thái giao dịch thành `SUCCESS` và trạng thái đặt bàn thành `CONFIRMED`.<br>10. Frontend (sau khi polling/nhận kết quả từ Backend) hiển thị màn hình: *"Thanh toán đặt cọc thành công. Bàn của bạn đã được giữ!"*<br>11. Use case kết thúc. |
| **Alternative flows**<br>(Luồng thay thế) | **2a. Khách hàng sử dụng Voucher:**<br>1. Tại bước 2, Khách hàng nhấn vào mục "Mã giảm giá / Voucher".<br>2. Khách hàng nhập mã (VD: `GIAM50K`) hoặc chọn từ danh sách voucher của mình.<br>3. Backend kiểm tra tính hợp lệ của mã (còn hạn, áp dụng cho nhà hàng này, đạt giá trị tối thiểu).<br>4. Nếu hợp lệ, hệ thống tính toán lại "Số tiền cọc cần thanh toán" (Trừ đi số tiền được giảm).<br>5. Frontend hiển thị số tiền mới. Luồng hoạt động tiếp tục tại Bước 3.<br><br>**6a. Khách hàng chủ động hủy thanh toán:**<br>1. Tại bước 5 hoặc 6, Khách hàng đổi ý, nhấn nút "Hủy giao dịch" hoặc "Quay lại" trên giao diện của Momo.<br>2. Momo chuyển hướng Khách hàng về lại ứng dụng với trạng thái hủy.<br>3. Frontend hiển thị thông báo: *"Thanh toán bị hủy. Đơn đặt bàn của bạn sẽ được giữ trong 15 phút để bạn thanh toán lại."*<br>4. Khách hàng có thể nhấn "Thanh toán lại" (Hệ thống tạo URL Momo mới - Quay lại Bước 4). |
| **Exception flows**<br>(Luồng ngoại lệ) | **3a (Luồng 2a). Lỗi mã giảm giá không hợp lệ (Validation Error):**<br>1. Tại bước 3 của luồng 2a, Backend phát hiện mã giảm giá đã hết hạn, sai mã, hoặc không đủ điều kiện áp dụng.<br>2. Backend trả về lỗi `400 Bad Request`.<br>3. Frontend hiển thị thông báo chữ đỏ: *"Mã giảm giá không hợp lệ hoặc đã hết hạn."*<br>4. Khách hàng bắt buộc phải xóa mã hoặc nhập mã khác để tiếp tục thanh toán.<br><br>**4a. Lỗi Cổng thanh toán Momo (Payment Gateway Timeout/Down):**<br>1. Tại bước 4 (tạo URL) hoặc bước 8 (nhận IPN), hệ thống Momo không phản hồi hoặc báo lỗi `502 Bad Gateway` / `500 Internal Server Error`.<br>2. Backend ghi log lỗi hệ thống và trả về thông báo cho Frontend.<br>3. Frontend hiển thị: *"Cổng thanh toán đang bảo trì hoặc mất kết nối. Vui lòng thử lại sau ít phút."*<br>4. Use case kết thúc thất bại.<br><br>**8a. Quá hạn thanh toán (Payment Timeout Logic):**<br>1. Khách hàng ở lại trang Momo quá lâu (VD: > 15 phút) mà không thanh toán.<br>2. Một Cronjob (tác vụ nền) của Backend quét các đơn `AWAITING_PAYMENT` quá hạn.<br>3. Backend tự động cập nhật trạng thái đơn thành `CANCELLED` để giải phóng bàn cho khách khác.<br>4. Nếu khách hàng thanh toán thành công *sau* khi đơn đã bị hủy, Backend nhận IPN sẽ ghi nhận giao dịch là `REFUND_PENDING` (Cần hoàn tiền) và gửi cảnh báo cho Admin xử lý.<br><br>**9a. Giả mạo giao dịch (IPN Forgery / Security Threat):**<br>1. Tại bước 8, một kẻ tấn công (Hacker) giả mạo request IPN gửi đến Backend báo rằng "Đã thanh toán thành công 200k".<br>2. Backend tính toán lại chữ ký (Signature/Checksum) dựa trên Secret Key của Momo và phát hiện chữ ký không khớp.<br>3. Backend từ chối request ngay lập tức (`403 Forbidden`), không cập nhật trạng thái đơn.<br>4. Ghi log cảnh báo bảo mật (Security Alert). |

---

*Wireframe tương ứng:*
![Wireframe Thanh toán](./screenshots/wireframes/customer/customer-06-booking-step3-payment.png)

![Wireframe Trạng thái thanh toán](./screenshots/wireframes/customer/customer-07-booking-result.png)

*Bổ sung các Wireframe khác:*

![Wireframe Trang cá nhân](./screenshots/wireframes/customer/customer-10-profile.png
)

---

### 3.2. Phân hệ Quản trị viên (Admin)
#### 3.2.1. Sơ đồ Use Case Admin
![Sơ đồ Use Case Admin](./screenshots/usecase-admin.png)

#### 3.2.2. Chi tiết Đặc tả & Wireframes
**1. UC_ADMIN_01: Duyệt nhà hàng mới**
| Trường thông tin | Nội dung |
| :--- | :--- |
| **Use case Id** | UC_ADMIN_01 |
| **Name**<br>(Tên use case) | Duyệt nhà hàng mới |
| **Description**<br>(Mô tả) | Use case này mô tả quy trình Quản trị viên (Admin) xem xét yêu cầu đăng ký mở gian hàng từ một đối tác Nhà hàng mới. Admin sẽ kiểm tra các thông tin pháp lý, hình ảnh, thực đơn dự kiến và đưa ra quyết định Phê duyệt (kèm thiết lập mức hoa hồng), Từ chối, hoặc Yêu cầu bổ sung thông tin. |
| **Primary Actor**<br>(Actor chính) | Admin (Quản trị hệ thống) |
| **Secondary Actor**<br>(Actor phụ) | Hệ thống gửi Email (Ví dụ: JavaMail / SendGrid) |
| **Pre-conditions**<br>(Tiền điều kiện) | 1. Admin đã đăng nhập thành công vào hệ thống quản trị (Admin Dashboard).<br>2. Có ít nhất một yêu cầu đăng ký nhà hàng mới đang ở trạng thái chờ duyệt (`PENDING`) trong cơ sở dữ liệu. |
| **Post-conditions**<br>(Hậu điều kiện) | **Thành công:**<br>1. Trạng thái của nhà hàng được cập nhật thành `APPROVED` (Đã duyệt) hoặc `REJECTED` (Từ chối) trong CSDL.<br>2. Mức chiết khấu (commission) được lưu lại (nếu duyệt).<br>3. Một email thông báo kết quả được gửi tự động đến chủ nhà hàng.<br><br>**Thất bại:**<br>1. Trạng thái của nhà hàng không thay đổi.<br>2. Hệ thống hiển thị thông báo lỗi cụ thể cho Admin. |
| **Main flows**<br>(Luồng hoạt động chính) | 1. Use case bắt đầu khi Admin chọn mục "Quản lý đối tác" -> "Yêu cầu chờ duyệt" trên thanh điều hướng.<br>2. Hệ thống truy vấn CSDL và hiển thị danh sách các nhà hàng đang ở trạng thái `PENDING`.<br>3. Admin nhấn vào nút "Xem chi tiết" tại một nhà hàng cụ thể.<br>4. Hệ thống hiển thị giao diện chi tiết hồ sơ nhà hàng bao gồm: Thông tin chủ sở hữu, Giấy phép kinh doanh (file ảnh/PDF), Địa chỉ, Hình ảnh không gian, và Thực đơn dự kiến.<br>5. Admin kiểm tra tính hợp lệ của các thông tin và tài liệu.<br>6. Admin nhấn nút **"Phê duyệt"**.<br>7. Hệ thống hiển thị một Dialog (hộp thoại) yêu cầu Admin nhập **Mức hoa hồng (Commission %)** áp dụng cho nhà hàng này và xác nhận.<br>8. Admin nhập mức hoa hồng (ví dụ: 15%) và nhấn "Xác nhận duyệt".<br>9. Hệ thống gửi yêu cầu cập nhật (POST/PUT) xuống Backend.<br>10. Backend cập nhật trạng thái nhà hàng thành `APPROVED` và lưu mức hoa hồng vào CSDL.<br>11. Backend kích hoạt một tác vụ nền (background task) gọi đến Hệ thống gửi Email để gửi thư chúc mừng và hướng dẫn đăng nhập cho Chủ nhà hàng.<br>12. Hệ thống hiển thị thông báo "Đã phê duyệt nhà hàng thành công" và đưa Admin quay lại danh sách chờ duyệt (Bước 2).<br>13. Use case kết thúc. |
| **Alternative flows**<br>(Luồng thay thế) | **6a. Admin quyết định Từ chối nhà hàng:**<br>1. Tại bước 6, Admin nhấn nút **"Từ chối"**.<br>2. Hệ thống hiển thị Dialog yêu cầu nhập "Lý do từ chối" (Bắt buộc).<br>3. Admin nhập lý do (ví dụ: Giấy phép kinh doanh mờ, không hợp lệ) và xác nhận.<br>4. Backend cập nhật trạng thái thành `REJECTED` và lưu lý do vào CSDL.<br>5. Backend gọi Hệ thống gửi Email để gửi thư thông báo từ chối kèm lý do cho Chủ nhà hàng.<br>6. Luồng hoạt động tiếp tục tại Bước 12.<br><br>**6b. Admin Yêu cầu bổ sung thông tin:**<br>1. Tại bước 6, Admin nhấn nút **"Yêu cầu bổ sung"**.<br>2. Hệ thống hiển thị Dialog yêu cầu nhập "Nội dung cần bổ sung".<br>3. Admin nhập nội dung (ví dụ: Cập nhật lại ảnh mặt tiền nhà hàng) và xác nhận.<br>4. Backend cập nhật trạng thái thành `UPDATE_REQUIRED`.<br>5. Backend gọi Hệ thống gửi Email thông báo cho Chủ nhà hàng vào cập nhật lại hồ sơ.<br>6. Luồng hoạt động tiếp tục tại Bước 12. |
| **Exception flows**<br>(Luồng ngoại lệ) | **10a. Lỗi kết nối Cơ sở dữ liệu (Database Error):**<br>1. Tại bước 10 (hoặc bước 4 của luồng 6a/6b), nếu CSDL gặp sự cố (timeout, deadlock).<br>2. Backend bắt lỗi (catch exception), hủy bỏ giao dịch (rollback) để đảm bảo tính toàn vẹn dữ liệu.<br>3. Backend trả về mã lỗi 500 Internal Server Error.<br>4. Hệ thống hiển thị thông báo: *"Lỗi hệ thống: Không thể cập nhật trạng thái lúc này. Vui lòng thử lại sau."*<br>5. Use case kết thúc trong trạng thái thất bại.<br><br>**11a. Lỗi Hệ thống gửi Email (Email Service Down):**<br>1. Tại bước 11, tác vụ nền không thể kết nối với máy chủ SMTP (JavaMail lỗi).<br>2. Backend ghi log lỗi hệ thống (Error Logging) và đưa tác vụ gửi email vào hàng đợi để thử lại sau (Retry Queue).<br>3. Trạng thái nhà hàng **vẫn được lưu thành công** trong CSDL.<br>4. Hệ thống hiển thị thông báo cảnh báo (Warning) cho Admin: *"Đã duyệt nhà hàng thành công, nhưng hệ thống gửi email đang gián đoạn. Email sẽ được gửi lại sau."*<br>5. Luồng hoạt động tiếp tục tại Bước 12. |

*Wireframe tương ứng:*
![Wireframe Duyệt nhà hàng](./screenshots/wireframes/admin/admin-02-approval-list.png)
![Wireframe Duyệt nhà hàng](./screenshots/wireframes/admin/admin-03-approval-detail.png)

**2. UC_ADMIN_02: Quản lý nhà hàng (CRUD)**
| Trường thông tin | Nội dung |
| :--- | :--- |
| **Use case Id** | UC_ADMIN_02 |
| **Name**<br>(Tên use case) | Quản lý nhà hàng (CRUD) |
| **Description**<br>(Mô tả) | Use case này cho phép Quản trị viên (Admin) xem danh sách toàn bộ nhà hàng đang hoạt động trên nền tảng. Admin có thể tìm kiếm, lọc, xem chi tiết, thêm mới nhà hàng thủ công, cập nhật thông tin (trạng thái hoạt động, mức hoa hồng) và khóa/xóa tài khoản nhà hàng khi có vi phạm. |
| **Primary Actor**<br>(Actor chính) | Admin (Quản trị hệ thống) |
| **Secondary Actor**<br>(Actor phụ) | Hệ thống Cơ sở dữ liệu (PostgreSQL/MySQL) |
| **Pre-conditions**<br>(Tiền điều kiện) | 1. Admin đã đăng nhập thành công vào hệ thống quản trị (Admin Dashboard).<br>2. Admin có quyền (Role/Permission) truy cập module "Quản lý nhà hàng". |
| **Post-conditions**<br>(Hậu điều kiện) | **Thành công:**<br>1. Các thay đổi (Thêm/Sửa/Xóa) được lưu trữ thành công vào CSDL.<br>2. Giao diện danh sách nhà hàng được cập nhật dữ liệu mới nhất.<br><br>**Thất bại:**<br>1. CSDL không bị thay đổi (Rollback nếu có lỗi).<br>2. Hệ thống hiển thị thông báo lỗi rõ ràng cho Admin. |
| **Main flows**<br>(Luồng hoạt động chính) | 1. Use case bắt đầu khi Admin chọn mục "Quản lý Nhà hàng" trên thanh menu điều hướng.<br>2. Hệ thống truy vấn CSDL và hiển thị danh sách tất cả các nhà hàng (phân trang), bao gồm các thông tin cơ bản: Tên, Chủ sở hữu, Trạng thái (Active/Inactive), Mức hoa hồng.<br>3. Admin nhấn vào nút **"Chỉnh sửa"** (hoặc Xem chi tiết) tại một nhà hàng cụ thể.<br>4. Hệ thống hiển thị form chi tiết chứa toàn bộ thông tin hiện tại của nhà hàng đó.<br>5. Admin tiến hành thay đổi thông tin (Ví dụ: Chỉnh sửa mức hoa hồng từ 15% lên 20%, hoặc thay đổi số điện thoại liên hệ).<br>6. Admin nhấn nút **"Lưu thay đổi"**.<br>7. Hệ thống (Frontend) kiểm tra tính hợp lệ của dữ liệu vừa nhập (Validate form).<br>8. Ứng dụng gửi yêu cầu (PUT/PATCH) kèm dữ liệu xuống Backend.<br>9. Backend cập nhật bản ghi tương ứng trong CSDL.<br>10. Hệ thống hiển thị thông báo "Cập nhật thông tin nhà hàng thành công" và quay lại màn hình danh sách (Bước 2).<br>11. Use case kết thúc. |
| **Alternative flows**<br>(Luồng thay thế) | **2a. Tìm kiếm và Lọc nhà hàng (Read):**<br>1. Tại bước 2, Admin nhập từ khóa (Tên nhà hàng) vào ô tìm kiếm hoặc chọn bộ lọc (Trạng thái: Đang hoạt động, Cuisine: Món Âu).<br>2. Hệ thống truy vấn lại CSDL dựa trên tham số lọc và cập nhật lại danh sách hiển thị.<br>3. Luồng hoạt động tiếp tục tại Bước 3.<br><br>**2b. Thêm mới nhà hàng thủ công (Create):**<br>1. Tại bước 2, Admin nhấn nút **"Thêm nhà hàng mới"**.<br>2. Hệ thống hiển thị một form trống.<br>3. Admin điền các thông tin bắt buộc (Tên, Địa chỉ, SĐT, Mức hoa hồng...).<br>4. Admin nhấn "Lưu".<br>5. Backend tạo bản ghi mới trong CSDL và tự động tạo tài khoản đăng nhập (Business User) cho chủ nhà hàng.<br>6. Luồng hoạt động tiếp tục tại Bước 10.<br><br>**3a. Khóa/Xóa nhà hàng (Delete/Deactivate):**<br>1. Tại bước 3, Admin nhấn nút **"Khóa/Vô hiệu hóa"** (Soft delete) tại một nhà hàng vi phạm.<br>2. Hệ thống hiển thị Dialog xác nhận: *"Bạn có chắc chắn muốn vô hiệu hóa nhà hàng này? Các đặt bàn sắp tới có thể bị ảnh hưởng."*<br>3. Admin nhấn "Xác nhận".<br>4. Backend cập nhật trạng thái nhà hàng thành `INACTIVE` (Không xóa cứng để giữ lịch sử dữ liệu).<br>5. Luồng hoạt động tiếp tục tại Bước 10. |
| **Exception flows**<br>(Luồng ngoại lệ) | **7a. Dữ liệu không hợp lệ từ phía Backend (Validation Error):**<br>1. Tại bước 8 (hoặc bước 4 của luồng 2b), nếu Admin nhập dữ liệu vi phạm ràng buộc CSDL (Ví dụ: Trùng số điện thoại đã tồn tại, hoặc mức hoa hồng nhập chữ thay vì số) mà Frontend lọt lưới.<br>2. Backend từ chối lưu và trả về mã lỗi `400 Bad Request`.<br>3. Hệ thống hiển thị thông báo lỗi màu đỏ ngay dưới trường dữ liệu bị sai (VD: *"Số điện thoại này đã được đăng ký cho nhà hàng khác"*).<br>4. Admin sửa lại dữ liệu và nhấn "Lưu" lần nữa.<br><br>**9a. Xung đột dữ liệu đồng thời (Concurrent Update Conflict):**<br>1. Tại bước 9, nếu có một Admin khác cũng đang chỉnh sửa và vừa lưu thành công thông tin của **cùng một nhà hàng** đó trước tích tắc.<br>2. Backend phát hiện phiên bản dữ liệu không khớp (Optimistic Locking).<br>3. Backend trả về mã lỗi `409 Conflict`.<br>4. Hệ thống hiển thị thông báo: *"Dữ liệu nhà hàng này vừa được cập nhật bởi một Quản trị viên khác. Vui lòng tải lại trang để xem thông tin mới nhất trước khi chỉnh sửa."*<br>5. Use case kết thúc thất bại, Admin phải F5 lại trang.<br><br>**9b. Mất kết nối Cơ sở dữ liệu:**<br>1. Tại bước 9, hệ thống không thể kết nối tới PostgreSQL/MySQL.<br>2. Backend trả về lỗi `500 Internal Server Error`.<br>3. Hệ thống hiển thị thông báo popup: *"Lỗi máy chủ: Không thể lưu dữ liệu lúc này. Vui lòng thử lại sau."* |

*Wireframe tương ứng:*
![Wireframe Quản lý nhà hàng](./screenshots/wireframes/admin/admin-04-restaurant-list.png)
![Wireframe Quản lý nhà hàng](./screenshots/wireframes/admin/admin-05-restaurant-form.png)

**3. UC_ADMIN_03: Quản lý danh mục cuisine**
| Trường thông tin | Nội dung |
| :--- | :--- |
| **Use case Id** | UC_ADMIN_03 |
| **Name**<br>(Tên use case) | Quản lý danh mục cuisine (CRUD) |
| **Description**<br>(Mô tả) | Use case này cho phép Quản trị viên (Admin) quản lý các danh mục ẩm thực (Cuisine Categories) trên hệ thống. Admin có thể xem danh sách, tìm kiếm, thêm mới, chỉnh sửa tên/hình ảnh đại diện, hoặc xóa các danh mục. Các danh mục này được dùng để nhà hàng tự phân loại và khách hàng sử dụng làm bộ lọc tìm kiếm. |
| **Primary Actor**<br>(Actor chính) | Admin (Quản trị hệ thống) |
| **Secondary Actor**<br>(Actor phụ) | Hệ thống Cơ sở dữ liệu (PostgreSQL/MySQL)<br>Dịch vụ lưu trữ hình ảnh (Ví dụ: AWS S3 / Cloudinary - *nếu có upload icon*) |
| **Pre-conditions**<br>(Tiền điều kiện) | 1. Admin đã đăng nhập thành công vào hệ thống quản trị.<br>2. Admin có quyền truy cập module "Cấu hình hệ thống" -> "Danh mục Cuisine". |
| **Post-conditions**<br>(Hậu điều kiện) | **Thành công:**<br>1. CSDL được cập nhật với các danh mục cuisine mới hoặc đã chỉnh sửa.<br>2. Ứng dụng của Khách hàng và Nhà hàng lập tức nhận được danh sách danh mục mới nhất.<br><br>**Thất bại:**<br>1. Trạng thái CSDL giữ nguyên.<br>2. Hệ thống hiển thị thông báo lỗi giải thích lý do không thể lưu/xóa. |
| **Main flows**<br>(Luồng hoạt động chính) | 1. Use case bắt đầu khi Admin chọn "Danh mục Cuisine" trên menu quản trị.<br>2. Hệ thống truy vấn CSDL và hiển thị danh sách các danh mục hiện có (Tên danh mục, Icon/Hình ảnh, Số lượng nhà hàng đang sử dụng).<br>3. Admin nhấn nút **"Chỉnh sửa"** tại một danh mục cụ thể (Ví dụ: "Món Nhật").<br>4. Hệ thống hiển thị form chứa thông tin hiện tại của danh mục.<br>5. Admin thay đổi thông tin (Ví dụ: Đổi tên thành "Ẩm thực Nhật Bản", tải lên icon mới).<br>6. Admin nhấn nút **"Lưu thay đổi"**.<br>7. Hệ thống (Frontend) kiểm tra tính hợp lệ (không được để trống tên).<br>8. Ứng dụng gửi yêu cầu (PUT/PATCH) xuống Backend.<br>9. Backend cập nhật bản ghi trong CSDL (và upload icon mới lên Cloudinary nếu có).<br>10. Hệ thống hiển thị thông báo: *"Cập nhật danh mục thành công"* và tải lại danh sách (Bước 2).<br>11. Use case kết thúc. |
| **Alternative flows**<br>(Luồng thay thế) | **2a. Thêm mới danh mục (Create):**<br>1. Tại bước 2, Admin nhấn nút **"Thêm danh mục mới"**.<br>2. Hệ thống hiển thị form trống.<br>3. Admin nhập Tên danh mục (VD: "Đồ ăn kiêng") và tải lên Icon.<br>4. Admin nhấn "Lưu".<br>5. Backend tạo bản ghi mới trong CSDL.<br>6. Luồng hoạt động tiếp tục tại Bước 10.<br><br>**3a. Xóa danh mục (Delete):**<br>1. Tại bước 3, Admin nhấn nút **"Xóa"** tại một danh mục **chưa có nhà hàng nào sử dụng** (Số lượng = 0).<br>2. Hệ thống hiển thị Dialog: *"Bạn có chắc chắn muốn xóa danh mục này vĩnh viễn?"*.<br>3. Admin nhấn "Xác nhận".<br>4. Backend thực hiện lệnh DELETE trong CSDL.<br>5. Luồng hoạt động tiếp tục tại Bước 10. |
| **Exception flows**<br>(Luồng ngoại lệ) | **3b. Lỗi Ràng buộc Khóa ngoại (Foreign Key Constraint) khi Xóa:**<br>1. Tại bước 3 của luồng 3a, Admin cố tình nhấn "Xóa" một danh mục **đang có nhà hàng sử dụng** (VD: Xóa "Món Việt" đang có 50 nhà hàng gắn tag).<br>2. Backend thực hiện lệnh DELETE nhưng bị CSDL từ chối do vi phạm khóa ngoại (Cuisine_ID đang tồn tại trong bảng Restaurant).<br>3. Backend bắt lỗi (catch exception) và trả về mã `409 Conflict` hoặc `400 Bad Request`.<br>4. Hệ thống hiển thị thông báo lỗi màu đỏ: *"Không thể xóa! Danh mục này đang được sử dụng bởi 50 nhà hàng. Vui lòng chuyển đổi danh mục cho các nhà hàng đó trước khi xóa."*<br>5. Use case kết thúc thất bại.<br><br>**8a. Lỗi Trùng lặp tên danh mục (Unique Constraint):**<br>1. Tại bước 8 (hoặc bước 4 của luồng 2a), Admin nhập tên danh mục đã tồn tại trong hệ thống (VD: Tạo mới danh mục "Hải sản" trong khi đã có "Hải sản").<br>2. Backend kiểm tra CSDL và phát hiện vi phạm ràng buộc Unique.<br>3. Backend trả về lỗi.<br>4. Hệ thống hiển thị cảnh báo: *"Tên danh mục này đã tồn tại. Vui lòng chọn tên khác."*<br>5. Admin phải sửa lại tên và thử lưu lại.<br><br>**9a. Lỗi dịch vụ lưu trữ ảnh (Cloudinary/S3 Down):**<br>1. Tại bước 9, Admin có tải lên Icon mới nhưng dịch vụ lưu trữ ảnh bên thứ 3 bị lỗi timeout.<br>2. Backend hủy bỏ giao dịch lưu CSDL (Rollback) để tránh việc có data nhưng mất ảnh.<br>3. Hệ thống thông báo: *"Lỗi tải ảnh lên máy chủ. Vui lòng kiểm tra lại file ảnh hoặc thử lại sau."* |

*Wireframe tương ứng:*
![Wireframe Quản lý danh mục](./screenshots/wireframes/admin/admin-06-cuisine-management.png)

**4. UC_ADMIN_04: Cấu hình commission**
| Trường thông tin | Nội dung |
| :--- | :--- |
| **Use case Id** | UC_ADMIN_04 |
| **Name**<br>(Tên use case) | Cấu hình commission (Mức hoa hồng) |
| **Description**<br>(Mô tả) | Use case này cho phép Quản trị viên cấp cao (Super Admin) thiết lập và thay đổi mức phần trăm hoa hồng (Commission Rate) mặc định mà nền tảng sẽ thu từ các nhà hàng trên mỗi đơn đặt bàn thành công. Hệ thống sẽ lưu lại lịch sử thay đổi để phục vụ đối soát doanh thu. Mức cấu hình này sẽ áp dụng cho các nhà hàng mới đăng ký hoặc các nhà hàng đang sử dụng mức mặc định. |
| **Primary Actor**<br>(Actor chính) | Super Admin (Quản trị viên cấp cao) |
| **Secondary Actor**<br>(Actor phụ) | Hệ thống Cơ sở dữ liệu (PostgreSQL/MySQL) |
| **Pre-conditions**<br>(Tiền điều kiện) | 1. Admin đã đăng nhập thành công vào hệ thống quản trị.<br>2. Admin phải có quyền hạn cao nhất (`ROLE_SUPER_ADMIN`) mới được phép truy cập module "Cấu hình tài chính". |
| **Post-conditions**<br>(Hậu điều kiện) | **Thành công:**<br>1. Mức hoa hồng mặc định mới được lưu vào CSDL (Bảng cấu hình hệ thống).<br>2. Một bản ghi lịch sử thay đổi (Audit Log) được tạo ra ghi rõ người thay đổi, thời gian, mức cũ và mức mới.<br>3. Các đơn đặt bàn phát sinh *sau thời điểm này* (đối với nhà hàng dùng mức mặc định) sẽ áp dụng mức hoa hồng mới.<br><br>**Thất bại:**<br>1. Trạng thái CSDL không thay đổi.<br>2. Hệ thống hiển thị thông báo lỗi giải thích lý do không thể lưu. |
| **Main flows**<br>(Luồng hoạt động chính) | 1. Use case bắt đầu khi Super Admin chọn "Cấu hình hệ thống" -> "Cấu hình Hoa hồng (Commission)" trên menu quản trị.<br>2. Hệ thống truy vấn CSDL và hiển thị Mức hoa hồng mặc định hiện tại (Ví dụ: 15%) cùng với Bảng lịch sử các lần thay đổi trước đó.<br>3. Super Admin nhấn nút **"Cập nhật mức hoa hồng"**.<br>4. Hệ thống hiển thị một Dialog (hộp thoại) yêu cầu nhập mức phần trăm mới.<br>5. Super Admin nhập giá trị mới (Ví dụ: 18%) và bắt buộc phải nhập "Lý do thay đổi" (Ví dụ: "Điều chỉnh chính sách năm 2026").<br>6. Super Admin nhấn nút **"Lưu cấu hình"**.<br>7. Hệ thống (Frontend) kiểm tra tính hợp lệ của dữ liệu (Giá trị phải là số dương, nằm trong khoảng 0 - 100, lý do không được để trống).<br>8. Ứng dụng gửi yêu cầu (PUT/POST) kèm dữ liệu xuống Backend.<br>9. Backend thực hiện một giao dịch CSDL (Database Transaction) bao gồm 2 bước:<br>&nbsp;&nbsp;&nbsp;a. Cập nhật bản ghi cấu hình hệ thống với mức hoa hồng mới (18%).<br>&nbsp;&nbsp;&nbsp;b. Tạo một bản ghi mới trong bảng Lịch sử (Audit Log) lưu lại: ID của Admin, Thời gian, Mức cũ (15%), Mức mới (18%), Lý do.<br>10. Backend trả về phản hồi thành công.<br>11. Hệ thống hiển thị thông báo: *"Cập nhật mức hoa hồng mặc định thành công"* và tải lại trang hiển thị mức mới cùng dòng lịch sử vừa thêm (Bước 2).<br>12. Use case kết thúc. |
| **Alternative flows**<br>(Luồng thay thế - *Nghiệp vụ hợp lệ*) | **5a. Hủy bỏ thao tác:**<br>1. Tại bước 5, Super Admin quyết định không thay đổi nữa và nhấn nút **"Hủy"** hoặc đóng Dialog.<br>2. Hệ thống đóng hộp thoại, không gửi yêu cầu nào xuống Backend.<br>3. Use case kết thúc.<br><br>**2a. Xem chi tiết lịch sử thay đổi:**<br>1. Tại bước 2, Super Admin muốn xem ai đã thay đổi mức hoa hồng vào tháng trước.<br>2. Super Admin sử dụng bộ lọc thời gian hoặc phân trang trên Bảng lịch sử.<br>3. Hệ thống truy vấn CSDL và hiển thị danh sách lịch sử tương ứng.<br>4. Luồng hoạt động tiếp tục tại Bước 3. |
| **Exception flows**<br>(Luồng ngoại lệ - *Ràng buộc DB, Phân quyền & Lỗi hệ thống*) | **8a. Lỗi Phân quyền (Unauthorized Access):**<br>1. Tại bước 8, nếu một Admin thông thường (không phải Super Admin) cố tình gửi yêu cầu API cập nhật hoa hồng (có thể qua Postman hoặc lỗi hiển thị UI).<br>2. Backend kiểm tra Token (JWT) và phát hiện user không có quyền `ROLE_SUPER_ADMIN`.<br>3. Backend từ chối yêu cầu và trả về mã lỗi `403 Forbidden`.<br>4. Hệ thống hiển thị thông báo lỗi màu đỏ: *"Bạn không có quyền thực hiện thao tác này. Vui lòng liên hệ Quản trị viên cấp cao."*<br>5. Use case kết thúc thất bại.<br><br>**9a. Lỗi Giao dịch CSDL (Transaction Rollback):**<br>1. Tại bước 9, quá trình lưu cấu hình mới (bước 9a) thành công nhưng quá trình ghi log lịch sử (bước 9b) bị lỗi (do mất kết nối DB hoặc lỗi ràng buộc bảng log).<br>2. Nhờ cơ chế `transaction.atomic` (hoặc tương đương), Backend tự động hủy bỏ (Rollback) toàn bộ giao dịch. Mức hoa hồng sẽ quay về giá trị cũ (15%).<br>3. Backend trả về mã lỗi `500 Internal Server Error`.<br>4. Hệ thống hiển thị thông báo: *"Lỗi hệ thống: Không thể lưu cấu hình lúc này để đảm bảo tính toàn vẹn dữ liệu. Vui lòng thử lại sau."*<br>5. Use case kết thúc thất bại.<br><br>**7a. Lỗi Dữ liệu không hợp lệ (Validation Error):**<br>1. Tại bước 7, Super Admin nhập giá trị hoa hồng là "-5" hoặc "120".<br>2. Frontend phát hiện lỗi validation ngay lập tức (hoặc Backend trả về lỗi `400 Bad Request` nếu lọt qua Frontend).<br>3. Hệ thống chặn thao tác lưu và hiển thị cảnh báo dưới ô nhập liệu: *"Mức hoa hồng phải là số dương và không vượt quá 100%."*<br>4. Super Admin phải nhập lại giá trị hợp lệ. |

*Wireframe tương ứng:*
![Wireframe Cấu hình hoa hồng](./screenshots/wireframes/admin/admin-07-system-config.png)

**5. UC_ADMIN_05: Báo cáo toàn hệ thống**
| Trường thông tin | Nội dung |
| :--- | :--- |
| **Use case Id** | UC_ADMIN_05 |
| **Name**<br>(Tên use case) | Báo cáo toàn hệ thống (Dashboard & Export) |
| **Description**<br>(Mô tả) | Use case này cho phép Quản trị viên (Admin) xem bức tranh tổng quan về hoạt động kinh doanh của toàn bộ nền tảng. Admin có thể xem các chỉ số (Tổng doanh thu hoa hồng, Tổng số lượt đặt bàn, Top nhà hàng nổi bật...), lọc dữ liệu theo khoảng thời gian tùy chỉnh và xuất báo cáo ra file (Excel/PDF) để lưu trữ hoặc phân tích thêm. |
| **Primary Actor**<br>(Actor chính) | Admin (Quản trị hệ thống) |
| **Secondary Actor**<br>(Actor phụ) | Hệ thống Cơ sở dữ liệu (PostgreSQL/MySQL)<br>Thư viện tạo file (Ví dụ: Apache POI cho Excel, iText/PDFBox cho PDF) |
| **Pre-conditions**<br>(Tiền điều kiện) | 1. Admin đã đăng nhập thành công vào hệ thống quản trị.<br>2. Admin có quyền truy cập module "Báo cáo & Thống kê". |
| **Post-conditions**<br>(Hậu điều kiện) | **Thành công:**<br>1. Admin xem được các biểu đồ/số liệu thống kê chính xác trên màn hình.<br>2. File báo cáo (Excel/PDF) được tạo thành công và tải xuống máy tính của Admin.<br><br>**Thất bại:**<br>1. Hệ thống không hiển thị được dữ liệu hoặc không tạo được file.<br>2. Hệ thống hiển thị thông báo lỗi (VD: Dữ liệu quá lớn, Lỗi máy chủ). |
| **Main flows**<br>(Luồng hoạt động chính - *Xem và Lọc dữ liệu*) | 1. Use case bắt đầu khi Admin chọn mục "Báo cáo thống kê" trên thanh menu điều hướng.<br>2. Hệ thống mặc định gửi yêu cầu lấy dữ liệu thống kê của **Tháng hiện tại**.<br>3. Backend truy vấn CSDL, tính toán tổng hợp (SUM, COUNT, GROUP BY) và trả về dữ liệu dạng JSON.<br>4. Hệ thống (Frontend) hiển thị Dashboard bao gồm:<br>&nbsp;&nbsp;&nbsp;- Các thẻ số liệu (Tổng doanh thu, Tổng đơn đặt bàn thành công/hủy).<br>&nbsp;&nbsp;&nbsp;- Biểu đồ đường (Line chart) thể hiện xu hướng đặt bàn theo ngày.<br>&nbsp;&nbsp;&nbsp;- Danh sách "Top 5 nhà hàng có doanh thu cao nhất".<br>5. Admin chọn một khoảng thời gian khác (Ví dụ: "Từ 01/01/2026 đến 31/03/2026") và nhấn nút **"Lọc dữ liệu"**.<br>6. Hệ thống gửi yêu cầu mới với tham số `start_date` và `end_date` xuống Backend.<br>7. Backend tính toán lại và trả về kết quả mới.<br>8. Giao diện Dashboard cập nhật lại các biểu đồ và con số tương ứng với Quý 1/2026.<br>9. Use case kết thúc. |
| **Alternative flows**<br>(Luồng thay thế - *Xuất file và Dữ liệu rỗng*) | **4a. Xuất báo cáo ra file (Export):**<br>1. Tại bước 4 (hoặc bước 8), Admin nhấn nút **"Xuất báo cáo"** và chọn định dạng **"Excel"**.<br>2. Hệ thống gửi yêu cầu Export xuống Backend kèm theo khoảng thời gian đang lọc.<br>3. Backend truy vấn dữ liệu chi tiết và sử dụng thư viện để ghi dữ liệu vào file `.xlsx`.<br>4. Backend trả về luồng dữ liệu file (File Stream) hoặc URL tải file.<br>5. Trình duyệt của Admin tự động tải file Excel xuống máy tính.<br>6. Use case kết thúc.<br><br>**7a. Không có dữ liệu trong khoảng thời gian đã chọn:**<br>1. Tại bước 7, Backend truy vấn nhưng không có bất kỳ giao dịch đặt bàn nào trong khoảng thời gian Admin chọn (VD: Chọn ngày trong tương lai).<br>2. Backend trả về mảng dữ liệu rỗng `[]` và các chỉ số bằng `0`.<br>3. Hệ thống hiển thị thông báo thân thiện trên Dashboard: *"Không có dữ liệu giao dịch nào trong khoảng thời gian này."* và ẩn các biểu đồ.<br>4. Luồng hoạt động quay lại Bước 5 (Chờ Admin chọn ngày khác). |
| **Exception flows**<br>(Luồng ngoại lệ - *Lỗi hiệu năng và Hệ thống*) | **6a. Lỗi khoảng thời gian quá lớn (Validation/Performance Constraint):**<br>1. Tại bước 6, Admin cố tình chọn khoảng thời gian quá dài (Ví dụ: Lọc dữ liệu của 5 năm qua).<br>2. Backend kiểm tra logic (Validation) và nhận thấy khoảng thời gian vượt quá giới hạn cho phép truy vấn trực tiếp (VD: Giới hạn tối đa là 1 năm/lần lọc để tránh treo DB).<br>3. Backend từ chối truy vấn và trả về mã lỗi `400 Bad Request`.<br>4. Hệ thống hiển thị cảnh báo: *"Khoảng thời gian lọc quá lớn. Vui lòng chọn khoảng thời gian tối đa là 12 tháng để đảm bảo tốc độ tải dữ liệu."*<br>5. Admin phải chọn lại ngày.<br><br>**7b. Lỗi Quá thời gian truy vấn (Database Timeout):**<br>1. Tại bước 7, do lượng dữ liệu trong CSDL quá khổng lồ (hàng triệu đơn đặt bàn), câu lệnh `GROUP BY` chạy quá lâu vượt mức cho phép (VD: > 30 giây).<br>2. CSDL tự động ngắt kết nối (Timeout) để giải phóng tài nguyên.<br>3. Backend trả về mã lỗi `504 Gateway Timeout`.<br>4. Hệ thống hiển thị thông báo: *"Hệ thống đang quá tải do lượng dữ liệu lớn. Vui lòng thử lại sau hoặc thu hẹp khoảng thời gian lọc."*<br><br>**3a (Luồng Export). Lỗi tạo file báo cáo (Out of Memory / File Generation Error):**<br>1. Tại bước 3 của luồng 4a, khi Backend đang tạo file Excel với hàng trăm ngàn dòng, máy chủ bị hết RAM (Out of Memory) hoặc thư viện lỗi.<br>2. Backend bắt lỗi (catch exception) và trả về mã `500 Internal Server Error`.<br>3. Hệ thống hiển thị thông báo: *"Đã xảy ra lỗi trong quá trình tạo file Excel. Vui lòng thử lại hoặc liên hệ bộ phận kỹ thuật."* |

*Wireframe tương ứng:*
![Wireframe Báo cáo Admin](./screenshots/wireframes/admin/admin-01-dashboard.png)

**6. UC_ADMIN_06: Quản lý thông báo**
| Trường thông tin | Nội dung |
| :--- | :--- |
| **Use case Id** | UC_ADMIN_06 |
| **Name**<br>(Tên use case) | Quản lý thông báo (Tạo và Gửi thông báo) |
| **Description**<br>(Mô tả) | Use case này cho phép Quản trị viên (Admin) soạn thảo và gửi các thông báo hệ thống (System Notifications) đến người dùng. Admin có thể chọn đối tượng nhận (Tất cả, Chỉ Khách hàng, Chỉ Nhà hàng, hoặc một User cụ thể), lên lịch gửi hoặc gửi ngay lập tức. Thông báo có thể được gửi qua App (Push Notification/In-app) hoặc Email. |
| **Primary Actor**<br>(Actor chính) | Admin (Quản trị hệ thống) |
| **Secondary Actor**<br>(Actor phụ) | Hệ thống Hàng đợi Tác vụ nền (Ví dụ: Celery + Redis)<br>Dịch vụ gửi thông báo (Ví dụ: Firebase Cloud Messaging - FCM / JavaMail) |
| **Pre-conditions**<br>(Tiền điều kiện) | 1. Admin đã đăng nhập thành công vào hệ thống quản trị.<br>2. Admin có quyền truy cập module "Quản lý Thông báo". |
| **Post-conditions**<br>(Hậu điều kiện) | **Thành công:**<br>1. Một bản ghi "Chiến dịch thông báo" (Notification Campaign) được lưu vào CSDL với trạng thái `SENT` (Đã gửi) hoặc `SCHEDULED` (Đã lên lịch).<br>2. Tác vụ gửi thông báo được đẩy vào Hàng đợi (Queue) thành công.<br><br>**Thất bại:**<br>1. Không có thông báo nào được gửi đi.<br>2. Hệ thống hiển thị lỗi rõ ràng cho Admin. |
| **Main flows**<br>(Luồng hoạt động chính - *Gửi thông báo ngay lập tức*) | 1. Use case bắt đầu khi Admin chọn "Quản lý Thông báo" -> "Tạo thông báo mới" trên menu.<br>2. Hệ thống hiển thị Form soạn thảo thông báo bao gồm: Tiêu đề, Nội dung, Đối tượng nhận (Tất cả/Khách hàng/Nhà hàng/Cá nhân), và Kênh gửi (App/Email).<br>3. Admin điền Tiêu đề (VD: "Bảo trì hệ thống đêm nay") và Nội dung chi tiết.<br>4. Admin chọn Đối tượng nhận là "Tất cả Nhà hàng" và Kênh gửi là "In-app Notification".<br>5. Admin nhấn nút **"Gửi thông báo"**.<br>6. Hệ thống (Frontend) kiểm tra tính hợp lệ (Tiêu đề/Nội dung không được rỗng).<br>7. Hệ thống hiển thị Dialog xác nhận: *"Bạn chuẩn bị gửi thông báo này đến [Số lượng] Nhà hàng. Xác nhận gửi?"*<br>8. Admin nhấn "Đồng ý".<br>9. Ứng dụng gửi payload (POST) xuống Backend.<br>10. Backend lưu bản ghi thông báo vào CSDL với trạng thái `PROCESSING` (Đang xử lý).<br>11. Backend đẩy tác vụ (Task) chứa nội dung và danh sách ID người nhận vào **Hàng đợi (Redis/Celery)** để xử lý ngầm (Background processing).<br>12. Backend trả về phản hồi thành công ngay lập tức cho Admin.<br>13. Hệ thống hiển thị thông báo: *"Thông báo đang được gửi đi trong nền."* và chuyển Admin về màn hình Lịch sử thông báo.<br>14. (Ngầm) Worker (Celery) lấy task từ hàng đợi, gọi API của Firebase (FCM) để đẩy thông báo đến các thiết bị, sau đó cập nhật trạng thái trong CSDL thành `SENT`.<br>15. Use case kết thúc. |
| **Alternative flows**<br>(Luồng thay thế - *Lên lịch và Xem lịch sử*) | **4a. Lên lịch gửi thông báo (Scheduled):**<br>1. Tại bước 4, Admin chọn thêm tùy chọn "Lên lịch gửi" và chọn thời gian (VD: 08:00 sáng mai).<br>2. Tại bước 10, Backend lưu bản ghi với trạng thái `SCHEDULED` (Đã lên lịch).<br>3. Tại bước 11, Backend đẩy tác vụ vào Hàng đợi có hẹn giờ (Scheduled Task - VD: Celery Beat).<br>4. Luồng hoạt động tiếp tục tại Bước 12.<br><br>**1a. Xem Lịch sử thông báo:**<br>1. Tại bước 1, Admin chọn "Lịch sử thông báo".<br>2. Hệ thống truy vấn CSDL và hiển thị danh sách các thông báo đã tạo, bao gồm Trạng thái (`SENT`, `PROCESSING`, `FAILED`, `SCHEDULED`) và Số lượng người đã nhận/đọc.<br>3. Admin có thể nhấn "Xem chi tiết" hoặc "Hủy" (đối với các thông báo đang `SCHEDULED`). |
| **Exception flows**<br>(Luồng ngoại lệ - *Lỗi Hàng đợi và Dịch vụ bên thứ 3*) | **11a. Lỗi kết nối Hàng đợi Tác vụ (Redis/Message Broker Down):**<br>1. Tại bước 11, Backend cố gắng đẩy tác vụ gửi thông báo vào Redis nhưng Redis bị sập (Connection Refused).<br>2. Backend bắt lỗi, cập nhật trạng thái bản ghi thông báo trong CSDL thành `FAILED` (Thất bại).<br>3. Backend trả về mã lỗi `500 Internal Server Error`.<br>4. Hệ thống hiển thị thông báo lỗi màu đỏ: *"Lỗi hệ thống: Không thể khởi tạo tiến trình gửi thông báo. Vui lòng liên hệ kỹ thuật."*<br>5. Use case kết thúc thất bại.<br><br>**14a. Lỗi Dịch vụ Firebase/Email (FCM/SMTP Error) trong lúc chạy ngầm:**<br>1. Tại bước 14 (tiến trình chạy ngầm), Worker đang gửi thông báo thì dịch vụ Firebase (FCM) báo lỗi (VD: Quá giới hạn API rate limit, hoặc Token thiết bị hết hạn).<br>2. Worker ghi log lỗi cụ thể cho từng User bị thất bại.<br>3. Nếu lỗi toàn hệ thống (FCM sập), Worker sẽ đưa task trở lại Hàng đợi để thử lại sau (Retry mechanism).<br>4. Nếu thử lại quá số lần quy định (Max retries), Worker cập nhật trạng thái chiến dịch trong CSDL thành `PARTIALLY_SENT` (Gửi một phần) hoặc `FAILED`.<br>*(Lưu ý: Vì đây là tiến trình ngầm, Admin sẽ không thấy lỗi ngay lập tức trên màn hình, mà chỉ thấy trạng thái `FAILED` khi xem lại Lịch sử thông báo ở luồng 1a).* |

*Wireframe tương ứng:*
![Wireframe Quản lý thông báo](./screenshots/wireframes/admin/admin-08-notification.png)

---

### 3.3. Phân hệ Nhà hàng (Restaurant / Business User)
#### 3.3.1. Sơ đồ Use Case Nhà hàng
![Sơ đồ Use Case Nhà hàng](./screenshots/Usecase-Business_User.png)

#### 3.3.2. Chi tiết Đặc tả & Wireframes
**1. UC_RESTAURANT_01: Quản lý thông tin nhà hàng**
| Trường thông tin | Nội dung |
| :--- | :--- |
| **Use case Id** | UC_RESTAURANT_01 |
| **Name**<br>(Tên use case) | Quản lý thông tin nhà hàng |
| **Description**<br>(Mô tả) | Use case này cho phép Chủ nhà hàng (hoặc Quản lý) xem và chỉnh sửa hồ sơ hiển thị của nhà hàng trên nền tảng. Quá trình này bắt buộc bao gồm việc cập nhật các thông tin cơ bản (Tên, Địa chỉ, Số điện thoại, Giờ mở cửa, Hình ảnh, Mô tả) để đảm bảo Khách hàng luôn tiếp cận được thông tin chính xác nhất. |
| **Primary Actor**<br>(Actor chính) | Nhà Hàng (Business User) |
| **Secondary Actor**<br>(Actor phụ) | Hệ thống Cơ sở dữ liệu (PostgreSQL/MySQL)<br>Dịch vụ lưu trữ hình ảnh (Cloudinary/AWS S3) |
| **Pre-conditions**<br>(Tiền điều kiện) | 1. Chủ nhà hàng đã đăng nhập thành công vào trang Quản trị dành cho đối tác (Restaurant Dashboard).<br>2. Tài khoản nhà hàng đang ở trạng thái hoạt động (Đã được Admin phê duyệt). |
| **Post-conditions**<br>(Hậu điều kiện) | **Thành công:**<br>1. Dữ liệu mới của nhà hàng được cập nhật thành công vào CSDL.<br>2. Ứng dụng của Khách hàng lập tức hiển thị thông tin mới nhất của nhà hàng này.<br><br>**Thất bại:**<br>1. Dữ liệu trong CSDL không thay đổi.<br>2. Hệ thống hiển thị thông báo lỗi cụ thể. |
| **Main flows**<br>(Luồng hoạt động chính) | 1. Use case bắt đầu khi Chủ nhà hàng chọn mục "Hồ sơ nhà hàng" (hoặc "Quản lý thông tin") trên thanh điều hướng của Dashboard.<br>2. Frontend gửi yêu cầu `GET` xuống Backend.<br>3. Backend truy vấn CSDL và trả về toàn bộ thông tin hiện tại của nhà hàng.<br>4. Frontend hiển thị Form thông tin bao gồm các trường: Tên nhà hàng, SĐT hotline, Địa chỉ, Giờ mở cửa/đóng cửa, Mô tả, và Khu vực tải ảnh (Logo, Ảnh bìa, Ảnh không gian).<br>5. Chủ nhà hàng tiến hành thay đổi các thông tin cần thiết.<br>6. Chủ nhà hàng nhấn nút **"Lưu thay đổi"**.<br>7. Frontend kiểm tra tính hợp lệ của các trường bắt buộc (Tên, SĐT, Địa chỉ không được để trống). Sau đó, gửi yêu cầu `PUT/PATCH` chứa payload dữ liệu mới xuống Backend.<br>8. Backend tiếp nhận, kiểm tra lại logic nghiệp vụ (Validation) và thực hiện lệnh `UPDATE` vào CSDL.<br>9. Backend trả về phản hồi thành công.<br>10. Frontend hiển thị thông báo: *"Cập nhật thông tin nhà hàng thành công!"* và làm mới lại giao diện với dữ liệu vừa lưu.<br>11. Use case kết thúc. |
| **Alternative flows**<br>(Luồng thay thế ) | **5a. Cập nhật hình ảnh nhà hàng:**<br>1. Tại bước 5, Chủ nhà hàng muốn thay đổi ảnh bìa.<br>2. Chủ nhà hàng nhấn "Tải ảnh lên" và chọn file từ máy tính.<br>3. Frontend hiển thị bản xem trước của ảnh mới.<br>4. Tại bước 7, Frontend gửi file ảnh cùng với dữ liệu text xuống Backend.<br>5. Backend gọi API của **Actor Dịch vụ lưu trữ ảnh (Cloudinary)** để upload file.<br>6. Cloudinary trả về URL an toàn của ảnh mới.<br>7. Backend lưu URL này vào CSDL thay cho URL cũ.<br>8. Luồng hoạt động tiếp tục tại Bước 9.<br><br>**6a. Hủy bỏ thao tác chỉnh sửa:**<br>1. Tại bước 5, Chủ nhà hàng chọn thay đổi thông tin nhưng đổi ý, nhấn nút "Hủy" hoặc click sang một trang khác trên menu.<br>2. Frontend phát hiện form đã bị thay đổi và hiển thị Dialog cảnh báo: *"Bạn có những thay đổi chưa được lưu. Bạn có chắc chắn muốn rời đi?"*<br>3. Chủ nhà hàng nhấn "Đồng ý rời đi".<br>4. Frontend hủy bỏ các thay đổi tạm thời, không gọi API, và chuyển hướng sang trang khác.<br>5. Use case kết thúc. |
| **Exception flows**<br>(Luồng ngoại lệ) | **7a. Lỗi logic dữ liệu (Bypass Validation / API Hacking):**<br>1. Tại bước 7, khi bị can thiệp vào API, gửi dữ liệu phi logic (Ví dụ: Giờ đóng cửa `18:00` lại sớm hơn Giờ mở cửa `20:00`, hoặc cố tình đổi `restaurant_id` sang nhà hàng khác).<br>2. Backend phát hiện vi phạm logic nghiệp vụ hoặc vi phạm phân quyền (Unauthorized).<br>3. Backend chặn lệnh UPDATE, trả về mã lỗi `400 Bad Request` hoặc `403 Forbidden`.<br>4. Frontend hiển thị thông báo lỗi màu đỏ: *"Dữ liệu không hợp lệ. Giờ đóng cửa phải sau giờ mở cửa."* hoặc *"Bạn không có quyền chỉnh sửa thông tin này."*<br>5. Use case kết thúc thất bại.<br><br>**5b (Luồng 5a). Lỗi Dịch vụ lưu trữ ảnh (Cloudinary Timeout):**<br>1. Tại bước 5 của luồng 5a, Backend gọi API upload ảnh nhưng Cloudinary bị sập hoặc nghẽn mạng.<br>2. Backend bắt lỗi (catch exception), hủy bỏ toàn bộ giao dịch cập nhật (Rollback) để tránh việc lưu text mới nhưng mất ảnh.<br>3. Backend trả về mã `502 Bad Gateway`.<br>4. Frontend hiển thị thông báo: *"Hệ thống tải ảnh đang gặp sự cố. Vui lòng thử lại sau ít phút."*<br><br>**8a. Lỗi mất kết nối Cơ sở dữ liệu (Database Error):**<br>1. Tại bước 8, Backend không thể lưu bản ghi vào PostgreSQL/MySQL do CSDL bị sập.<br>2. Backend trả về mã `500 Internal Server Error`.<br>3. Frontend hiển thị thông báo: *"Hệ thống đang bận, không thể lưu thay đổi lúc này."* |

---
*Wireframe tương ứng:*
![Wireframe Hồ sơ nhà hàng](./screenshots/wireframe-rest-profile.png)

**2. UC_RESTAURANT_02: Quản lý thực đơn**
| Trường thông tin | Nội dung |
| :--- | :--- |
| **Use case Id** | UC_RESTAURANT_02 |
| **Name**<br>(Tên use case) | Quản lý thực đơn (CRUD món ăn) |
| **Description**<br>(Mô tả) | Use case này cho phép Chủ nhà hàng (hoặc Quản lý) xem, thêm mới, chỉnh sửa, và xóa các món ăn/đồ uống trong thực đơn của mình. Quá trình này bắt buộc bao gồm chức năng bật/tắt trạng thái món ăn nhanh chóng. Ngoài ra, người dùng có thể mở rộng sang việc tạo/chỉnh sửa các danh mục món ăn (Categories) để phân nhóm thực đơn một cách khoa học. |
| **Primary Actor**<br>(Actor chính) | Nhà Hàng (Business User) |
| **Secondary Actor**<br>(Actor phụ) | Hệ thống Cơ sở dữ liệu (PostgreSQL/MySQL)<br>Dịch vụ lưu trữ hình ảnh (Cloudinary / AWS S3) |
| **Pre-conditions**<br>(Tiền điều kiện) | 1. Chủ nhà hàng đã đăng nhập thành công vào trang Quản trị (Restaurant Dashboard).<br>2. Tài khoản nhà hàng đang ở trạng thái hoạt động. |
| **Post-conditions**<br>(Hậu điều kiện) | **Thành công:**<br>1. Dữ liệu món ăn/danh mục mới được cập nhật thành công vào CSDL.<br>2. Ứng dụng của Khách hàng lập tức hiển thị thực đơn mới nhất.<br><br>**Thất bại:**<br>1. Không có thay đổi nào được lưu vào CSDL.<br>2. Hệ thống hiển thị thông báo lỗi cụ thể. |
| **Main flows**<br>(Luồng hoạt động chính - *Thêm/Sửa món ăn*) | 1. Use case bắt đầu khi Chủ nhà hàng chọn mục "Quản lý Thực đơn" trên thanh điều hướng.<br>2. Hệ thống truy vấn CSDL và hiển thị danh sách toàn bộ món ăn, được phân nhóm theo từng Danh mục (Khai vị, Món chính, Đồ uống).<br>3. Chủ nhà hàng nhấn nút **"Thêm món mới"** (hoặc "Chỉnh sửa" một món có sẵn).<br>4. Hệ thống hiển thị Form thông tin món ăn bao gồm: Tên món, Giá tiền, Hình ảnh minh họa, Mô tả thành phần, và Dropdown chọn Danh mục.<br>5. Chủ nhà hàng điền đầy đủ thông tin và tải lên hình ảnh món ăn.<br>6. Chủ nhà hàng nhấn nút **"Lưu món ăn"**.<br>7. Frontend kiểm tra tính hợp lệ (Tên món, Giá tiền không được trống).<br>8. Frontend gửi yêu cầu `POST/PUT` kèm dữ liệu (bao gồm cả file ảnh) xuống Backend.<br>9. Backend gọi API của **Actor Cloudinary** để lưu trữ hình ảnh và nhận lại URL.<br>10. Backend lưu toàn bộ thông tin món ăn (kèm URL ảnh) vào CSDL.<br>11. Hệ thống hiển thị thông báo: *"Đã lưu món ăn thành công!"* và tải lại danh sách thực đơn (Bước 2).<br>12. Use case kết thúc. |
| **Alternative flows**<br>(Luồng thay thế) | **2a. Thao tác nhanh (`Cập nhật trạng thái món`):**<br>1. Tại bước 2, ngay trên danh sách thực đơn, mỗi món ăn có một nút gạt (Toggle switch) thể hiện trạng thái: "Đang bán" (Available) hoặc "Hết hàng" (Sold out).<br>2. Khi bếp báo hết nguyên liệu, Chủ nhà hàng click vào nút gạt của món đó để chuyển sang "Hết hàng".<br>3. Frontend gửi ngay một yêu cầu `PATCH` (chỉ chứa ID món và trạng thái mới) xuống Backend.<br>4. Backend cập nhật trường `status` trong CSDL.<br>5. Hệ thống hiển thị thông báo nhỏ (Toast): *"Đã cập nhật trạng thái món ăn."* Khách hàng sẽ không thể đặt món này nữa.<br>6. Luồng hoạt động tiếp tục tại Bước 2.<br><br>**4a. Thêm nhóm món ăn mới (`Quản lý danh mục`):**<br>1. Tại bước 4, khi đang tạo món mới, Chủ nhà hàng nhận ra chưa có danh mục phù hợp (Muốn thêm món mới nhưng chưa có danh mục).<br>2. Chủ nhà hàng nhấn vào nút **"+ Thêm danh mục mới"** nằm cạnh Dropdown chọn danh mục.<br>3. Hệ thống hiển thị một Popup nhỏ yêu cầu nhập Tên danh mục.<br>4. Chủ nhà hàng nhập tên danh mục mới và nhấn Lưu.<br>5. Backend tạo bản ghi Danh mục mới trong CSDL và trả về ID.<br>6. Popup đóng lại, Dropdown tự động cập nhật và chọn sẵn danh mục vừa tạo.<br>7. Luồng hoạt động tiếp tục tại Bước 5. |
| **Exception flows**<br>(Luồng ngoại lệ ) | **8a. Lỗi logic dữ liệu (Bypass Validation / API Hacking):**<br>1. Tại bước 8, nếu cố tình dùng công cụ can thiệp API để truyền lên Giá tiền là số âm hoặc gán món ăn vào một ID Danh mục không thuộc về nhà hàng của mình.<br>2. Backend (thông qua Spring Validation và Logic Check) phát hiện dữ liệu phi lý hoặc vi phạm quyền sở hữu dữ liệu.<br>3. Backend chặn lệnh INSERT/UPDATE, trả về mã lỗi `400 Bad Request` hoặc `403 Forbidden`.<br>4. Frontend hiển thị thông báo lỗi: *"Giá tiền không hợp lệ"* hoặc *"Bạn không có quyền thêm món vào danh mục này."*<br>5. Use case kết thúc thất bại.<br><br>**9a. Lỗi Dịch vụ lưu trữ ảnh (Cloudinary Timeout):**<br>1. Tại bước 9, Backend gọi API upload ảnh món ăn nhưng Cloudinary bị sập hoặc nghẽn mạng.<br>2. Backend bắt lỗi (catch exception), hủy bỏ toàn bộ giao dịch (Rollback) để tránh việc tạo ra một món ăn bị lỗi hình ảnh (broken image) trên app của Khách hàng.<br>3. Backend trả về mã `502 Bad Gateway`.<br>4. Frontend hiển thị thông báo: *"Hệ thống tải ảnh đang gặp sự cố. Vui lòng thử lại sau ít phút."*<br><br>**10a. Lỗi mất kết nối Cơ sở dữ liệu (Database Error):**<br>1. Tại bước 10, Backend không thể lưu bản ghi vào PostgreSQL/MySQL do CSDL bị sập.<br>2. Backend trả về mã `500 Internal Server Error`.<br>3. Frontend hiển thị thông báo: *"Hệ thống đang bận, không thể lưu thực đơn lúc này."* |

---
*Wireframe tương ứng:*
![Wireframe Quản lý thực đơn](./screenshots/wireframe-rest-menu.png)

**3. UC_RESTAURANT_03: Quản lý bàn (Sơ đồ bàn)**
| Trường thông tin | Nội dung |
| :--- | :--- |
| **Use case Id** | UC_RESTAURANT_03 |
| **Name**<br>(Tên use case) | Quản lý bàn (Sơ đồ bàn) |
| **Description**<br>(Mô tả) | Use case này cho phép Nhân viên/Quản lý nhà hàng theo dõi và điều phối chỗ ngồi của khách hàng thông qua một sơ đồ bàn trực quan. Quá trình này bắt buộc phải bao gồm việc cập nhật trạng thái thực tế của từng bàn (Trống, Có khách, Đã đặt trước...). Ngoài ra, hệ thống hỗ trợ tính năng mở rộng để gộp hoặc tách các bàn khi có yêu cầu đặc biệt từ nhóm khách đông. |
| **Primary Actor**<br>(Actor chính) | Nhà Hàng (Business User / Lễ tân) |
| **Secondary Actor**<br>(Actor phụ) | Hệ thống Cơ sở dữ liệu (PostgreSQL/MySQL) |
| **Pre-conditions**<br>(Tiền điều kiện) | 1. Nhân viên nhà hàng đã đăng nhập thành công vào hệ thống (POS/Dashboard).<br>2. Nhà hàng đã thiết lập sẵn danh sách các bàn (Tên bàn, Sức chứa) trong CSDL. |
| **Post-conditions**<br>(Hậu điều kiện) | **Thành công:**<br>1. Sơ đồ bàn hiển thị chính xác trạng thái hiện tại của nhà hàng.<br>2. Các thay đổi trạng thái hoặc gộp/tách bàn được lưu vào CSDL và đồng bộ theo thời gian thực (nếu có dùng WebSocket).<br><br>**Thất bại:**<br>1. Trạng thái bàn không thay đổi.<br>2. Hệ thống hiển thị thông báo lỗi. |
| **Main flows**<br>(Luồng hoạt động chính - *Xem sơ đồ và Cập nhật trạng thái*) | 1. Use case bắt đầu khi Nhân viên chọn mục "Sơ đồ bàn" trên màn hình chính.<br>2. Frontend gửi yêu cầu `GET` xuống Backend để lấy danh sách toàn bộ bàn và trạng thái hiện tại của chúng.<br>3. Backend truy vấn CSDL và trả về danh sách bàn.<br>4. Frontend hiển thị giao diện Sơ đồ bàn trực quan, sử dụng màu sắc để phân biệt trạng thái (Xanh = Trống, Đỏ = Có khách, Vàng = Đã đặt).<br>5. Nhân viên quan sát sơ đồ để sắp xếp chỗ cho khách mới đến.<br>6. Khách hàng đến nhận Bàn (đã đặt trước). Nhân viên click vào biểu tượng Bàn trên sơ đồ.<br>7. Hệ thống hiển thị một Popup/Menu ngữ cảnh với các tùy chọn hành động.<br>8. Nhân viên chọn hành động **"Check-in (Đón khách)"**.<br>9. Frontend gửi yêu cầu `PATCH` (chứa `table_id` và `status = OCCUPIED`) xuống Backend.<br>10. Backend cập nhật trạng thái bàn trong CSDL và liên kết bàn này với đơn đặt bàn tương ứng (nếu có).<br>11. Backend trả về phản hồi thành công.<br>12. Frontend thay đổi màu sắc của Bàn 3 sang màu Đỏ (Có khách).<br>13. Use case kết thúc. |
| **Alternative flows**<br>(Luồng thay thế) | **8a. Bàn ăn xong, khách rời đi (`Cập nhật trạng thái bàn`):**<br>1. Tại bước 8, khách ở Bàn khác đã thanh toán xong và rời đi. Nhân viên click vào Bàn trống và chọn **"Dọn dẹp"**.<br>2. Backend cập nhật trạng thái thành `CLEANING` (Màu cam).<br>3. Sau khi dọn xong, nhân viên click lại và chọn **"Bàn trống"**.<br>4. Backend cập nhật trạng thái thành `AVAILABLE` (Màu xanh).<br>5. Luồng hoạt động tiếp tục tại Bước 11.<br><br>**8b. Khách đi nhóm đông (`Xử lý gộp/tách bàn`):**<br>1. Tại bước 8, một nhóm khách đến nhưng nhà hàng chỉ còn các bàn nhỏ không đủ cho nhóm.<br>2. Nhân viên click  chọn tính năng **"Gộp bàn" (Merge Tables)**.<br>3. Hệ thống hiển thị danh sách các bàn đang trống xung quanh. Nhân viên tick chọn Bàn và nhấn "Xác nhận gộp".<br>4. Frontend gửi yêu cầu `POST` xuống Backend để tạo một "Nhóm bàn tạm thời" (Table Group).<br>5. Backend cập nhật trạng thái cả 2 bàn thành `OCCUPIED` và liên kết chúng chung một mã hóa đơn (Order ID).<br>6. Sơ đồ bàn hiển thị các Bàn đã gộp cùng một viền màu.<br>7. *(Khi khách về, nhân viên chọn "Tách bàn" để trả 2 bàn về trạng thái độc lập ban đầu).*<br>8. Luồng hoạt động tiếp tục tại Bước 11. |
| **Exception flows**<br>(Luồng ngoại lệ) | **8c (Luồng 8b). Lỗi logic gộp bàn (Bypass Validation):**<br>1. Tại bước 4 của luồng 8b, Nhân viên (do nhầm lẫn hoặc lỗi UI) gửi lệnh gộp Bàn nhầm.<br>2. Backend kiểm tra logic nghiệp vụ và phát hiện Bàn đó không ở trạng thái `AVAILABLE`.<br>3. Backend chặn lệnh gộp bàn, trả về mã lỗi `409 Conflict` hoặc `400 Bad Request`.<br>4. Frontend hiển thị thông báo lỗi: *"Không thể gộp bàn. Bàn hiện đang có khách hoặc chưa được dọn dẹp."*<br>5. Nhân viên phải chọn lại bàn khác hợp lệ.<br><br>**9a. Xung đột cập nhật đồng thời (Race Condition):**<br>1. Tại bước 9, Lễ tân này vừa bấm "Check-in" cho Bàn. Cùng lúc đó (chênh lệch vài mili-giây), Lễ tân kia ở máy tính khác cũng bấm "Check-in" cho một nhóm khách vãng lai vào chính Bàn đó.<br>2. Backend (thông qua cơ chế Optimistic Locking hoặc Transaction) phát hiện trạng thái Bàn đã bị thay đổi bởi Lễ tân khác.<br>3. Backend từ chối yêu cầu của Lễ tân phản hồi sau và trả về mã lỗi `409 Conflict`.<br>4. Máy của Lễ tân sau hiển thị thông báo: *"Bàn vừa được nhân viên khác cập nhật. Vui lòng chọn bàn khác."* và tự động tải lại sơ đồ bàn mới nhất.<br><br>**10a. Lỗi mất kết nối Cơ sở dữ liệu (Database Error):**<br>1. Tại bước 10, Backend không thể lưu trạng thái bàn vào PostgreSQL/MySQL do CSDL bị sập.<br>2. Backend trả về mã `500 Internal Server Error`.<br>3. Frontend hiển thị thông báo: *"Hệ thống đang bận, không thể cập nhật trạng thái bàn lúc này."* |

---

*Wireframe tương ứng:*
![Wireframe Sơ đồ bàn](./screenshots/wireframe-rest-tables.png)

**4. UC_RESTAURANT_04: Quản lý thanh toán**
| Trường thông tin | Nội dung |
| :--- | :--- |
| **Use case Id** | UC_RESTAURANT_04 |
| **Name**<br>(Tên use case) | Quản lý thanh toán |
| **Description**<br>(Mô tả) | Use case này cho phép Thu ngân hoặc Quản lý nhà hàng thực hiện chốt bill và thu tiền của khách hàng sau khi dùng bữa. Quá trình này hỗ trợ nhiều phương thức thanh toán (Tiền mặt, Thẻ, hoặc qua cổng điện tử VNPay/Momo). Sau khi giao dịch thành công, hệ thống bắt buộc phải xuất hóa đơn (dưới dạng PDF/Excel) để in cho khách hoặc lưu trữ. |
| **Primary Actor**<br>(Actor chính) | Nhà Hàng (Thu ngân / Quản lý) |
| **Secondary Actor**<br>(Actor phụ) | Hệ thống Cơ sở dữ liệu (PostgreSQL/MySQL)<br>Cổng thanh toán (VNPay / Momo)<br>Trình tạo file (Pdf / Excel) |
| **Pre-conditions**<br>(Tiền điều kiện) | 1. Thu ngân đã đăng nhập thành công vào hệ thống POS/Dashboard của nhà hàng.<br>2. Có một bàn/đơn hàng đang ở trạng thái "Đang phục vụ" và khách hàng yêu cầu thanh toán (chốt bill). |
| **Post-conditions**<br>(Hậu điều kiện) | **Thành công:**<br>1. Trạng thái đơn hàng/bàn chuyển sang `PAID` (Đã thanh toán) và bàn được giải phóng (chuyển sang `CLEANING` hoặc `AVAILABLE`).<br>2. Doanh thu được ghi nhận vào CSDL.<br>3. Hóa đơn PDF/Excel được tạo thành công.<br><br>**Thất bại:**<br>1. Đơn hàng giữ nguyên trạng thái chưa thanh toán.<br>2. Hệ thống hiển thị thông báo lỗi. |
| **Main flows**<br>(Luồng hoạt động chính - *Thanh toán tiền mặt & Xuất hóa đơn*) | 1. Use case bắt đầu khi Thu ngân chọn một bàn đang có khách và nhấn nút **"Thanh toán"**.<br>2. Hệ thống truy vấn CSDL và hiển thị chi tiết Hóa đơn tạm tính (Danh sách món, Thuế VAT, Phí dịch vụ, Tiền cọc đã đóng nếu có, và Tổng tiền cần thanh toán).<br>3. Thu ngân xác nhận lại các món ăn với khách hàng.<br>4. Thu ngân chọn phương thức thanh toán là **"Tiền mặt"** (Cash) và nhập số tiền khách đưa.<br>5. Hệ thống tự động tính số tiền thối lại (tiền thừa).<br>6. Thu ngân nhấn nút **"Xác nhận thanh toán"**.<br>7. Backend cập nhật trạng thái đơn hàng thành `PAID` và lưu lịch sử giao dịch vào CSDL.<br>8. Ngay sau khi lưu CSDL thành công, Backend tự động gọi module xuất file.<br>9. Backend tương tác với **Actor Pdf/Excel** để render (tạo) một file hóa đơn định dạng `.pdf` chứa đầy đủ thông tin bill.<br>10. Backend trả về URL của file PDF cho Frontend.<br>11. Frontend hiển thị thông báo: *"Thanh toán thành công"* và tự động mở hộp thoại In hóa đơn (Print dialog) từ file PDF vừa tạo.<br>12. Use case kết thúc. |
| **Alternative flows**<br>(Luồng thay thế - *Thanh toán qua cổng điện tử*) | **4a. Thanh toán qua ví điện tử (`Tương tác với Actor VNPay/Momo`):**<br>1. Tại bước 4, Thu ngân chọn phương thức thanh toán là **"VNPay / Momo"**.<br>2. Backend tạo một mã giao dịch và gọi API của **Actor VNPay/Momo** để tạo mã QR thanh toán động (chứa sẵn số tiền).<br>3. Frontend hiển thị mã QR này lên màn hình (hoặc màn hình phụ hướng về phía khách).<br>4. Khách hàng sử dụng điện thoại quét mã QR và xác nhận thanh toán.<br>5. **Actor VNPay/Momo** xử lý giao dịch và gửi một Webhook (IPN) ngầm về Backend của hệ thống thông báo giao dịch thành công.<br>6. Backend nhận IPN, xác thực chữ ký bảo mật, và tự động cập nhật trạng thái đơn hàng thành `PAID`.<br>7. Frontend (đang polling hoặc dùng WebSocket) nhận được tín hiệu thành công, tự động chuyển sang màn hình hoàn tất.<br>8. Luồng hoạt động tiếp tục tại Bước 8 (Xuất hóa đơn).<br><br>**2a. Áp dụng mã giảm giá / Voucher tại quầy:**<br>1. Tại bước 2, khách hàng đưa mã giảm giá. Thu ngân nhập mã vào ô "Voucher / Giảm giá".<br>2. Backend kiểm tra tính hợp lệ của mã. Nếu hợp lệ, hệ thống tính toán lại Tổng tiền cuối cùng.<br>3. Luồng hoạt động tiếp tục tại Bước 3. |
| **Exception flows**<br>(Luồng ngoại lệ) | **5a (Luồng 4a). Lỗi Cổng thanh toán (VNPay/Momo Timeout):**<br>1. Tại bước 5 của luồng 4a, khách hàng đã quét mã và bị trừ tiền, nhưng hệ thống VNPay/Momo bị nghẽn mạng, không gửi IPN về Backend.<br>2. Frontend chờ quá thời gian quy định (Ví dụ: 3 phút) mà không thấy phản hồi.<br>3. Frontend hiển thị cảnh báo: *"Chưa nhận được xác nhận từ cổng thanh toán."*<br>4. Thu ngân nhấn nút **"Kiểm tra trạng thái giao dịch"**. Backend chủ động gọi API Query sang VNPay/Momo để hỏi trạng thái.<br>5. Nếu VNPay/Momo trả về lỗi `500` hoặc báo giao dịch thất bại, Thu ngân thỏa thuận với khách để thanh toán lại bằng Tiền mặt.<br><br>**8a. Lỗi tạo file Hóa đơn (Actor Pdf/Excel Crash):**<br>1. Tại bước 9, khi Backend đang tạo file PDF, máy chủ bị hết RAM (Out of Memory) hoặc thư viện tạo PDF bị lỗi.<br>2. Backend bắt lỗi (catch exception). **Lưu ý:** Giao dịch thanh toán (tiền) đã được lưu thành công ở bước 7, nên KHÔNG rollback CSDL.<br>3. Backend trả về mã lỗi `500 Internal Server Error` cho tiến trình tạo file.<br>4. Frontend hiển thị thông báo cảnh báo (Warning): *"Thanh toán thành công, nhưng hệ thống gặp lỗi khi tạo file in hóa đơn. Vui lòng thử in lại trong mục Lịch sử."*<br><br>**6a. Xung đột dữ liệu (Concurrent Modification / Race Condition):**<br>1. Tại bước 6, Thu ngân A nhấn "Xác nhận thanh toán". Cùng lúc đó (chênh vài mili-giây), một Nhân viên phục vụ B dùng tablet order thêm 1 món "Bia" vào chính bàn đó.<br>2. Backend kiểm tra tổng số tiền hoặc version của đơn hàng (Optimistic Locking) và phát hiện sự sai lệch.<br>3. Backend chặn thanh toán, trả về mã lỗi `409 Conflict`.<br>4. Frontend hiển thị thông báo: *"Đơn hàng vừa bị thay đổi (thêm/bớt món). Vui lòng tải lại trang để xem tổng tiền mới nhất trước khi thanh toán."* |

---

*Wireframe tương ứng:*
![Wireframe Thanh toán POS](./screenshots/wireframe-rest-payment.png)

**5. UC_RESTAURANT_05: Báo cáo doanh thu**
| Trường thông tin | Nội dung |
| :--- | :--- |
| **Use case Id** | UC_RESTAURANT_05 |
| **Name**<br>(Tên use case) | Báo cáo doanh thu |
| **Description**<br>(Mô tả) | Use case này cho phép Chủ nhà hàng hoặc Quản lý theo dõi hiệu quả kinh doanh thông qua các biểu đồ và số liệu thống kê (Tổng doanh thu, số đơn hoàn thành, số đơn hủy...). Quá trình này bắt buộc phải đi kèm thao tác lọc dữ liệu theo một khoảng thời gian nhất định. Ngoài ra, người dùng có thể mở rộng bằng cách xuất dữ liệu ra file (PDF/Excel) để lưu trữ hoặc báo cáo cấp trên. |
| **Primary Actor**<br>(Actor chính) | Nhà Hàng (Business User / Quản lý) |
| **Secondary Actor**<br>(Actor phụ) | Hệ thống Cơ sở dữ liệu (PostgreSQL/MySQL)<br>Trình tạo file (Actor Pdf / Excel) |
| **Pre-conditions**<br>(Tiền điều kiện) | 1. Quản lý nhà hàng đã đăng nhập thành công vào hệ thống (Restaurant Dashboard).<br>2. Tài khoản có quyền truy cập module "Báo cáo & Thống kê". |
| **Post-conditions**<br>(Hậu điều kiện) | **Thành công:**<br>1. Hệ thống hiển thị chính xác các biểu đồ, con số thống kê theo khoảng thời gian đã chọn.<br>2. File báo cáo được tạo và tải xuống thành công (nếu có yêu cầu).<br><br>**Thất bại:**<br>1. Giao diện không hiển thị được dữ liệu.<br>2. Hệ thống báo lỗi. |
| **Main flows**<br>(Luồng hoạt động chính - *Xem báo cáo mặc định*) | 1. Use case bắt đầu khi Quản lý chọn mục "Báo cáo doanh thu" trên thanh menu điều hướng.<br>2. Hệ thống (Frontend) tự động thiết lập khoảng thời gian mặc định là **"Tháng hiện tại"** (Từ ngày 1 đến ngày hiện tại) và gửi yêu cầu `GET` xuống Backend kèm tham số `start_date`, `end_date`.<br>3. Backend tiếp nhận yêu cầu, thực hiện truy vấn tổng hợp dữ liệu (Aggregation query: SUM doanh thu, COUNT số đơn) từ CSDL dựa trên ID của nhà hàng và khoảng thời gian.<br>4. Backend trả về dữ liệu dưới dạng JSON.<br>5. Frontend nhận dữ liệu và vẽ lên giao diện Dashboard bao gồm:<br>&nbsp;&nbsp;&nbsp;- Các thẻ tổng quát: Tổng doanh thu, Tổng số đơn, Tỉ lệ hủy bàn.<br>&nbsp;&nbsp;&nbsp;- Biểu đồ đường (Line chart): Biến động doanh thu theo từng ngày.<br>&nbsp;&nbsp;&nbsp;- Danh sách: Top món ăn bán chạy nhất.<br>6. Quản lý xem xét các số liệu trên màn hình.<br>7. Use case kết thúc. |
| **Alternative flows**<br>(Luồng thay thế - *Đoán trước được, có hướng xử lý/mở rộng*) | **2a. Thay đổi bộ lọc thời gian (`Lọc theo thời gian`):**<br>1. Tại bước 5, Quản lý muốn xem dữ liệu của tháng trước. Quản lý click vào bộ chọn ngày (Date Picker).<br>2. Quản lý chọn khoảng thời gian mới ("Tháng trước" hoặc tùy chọn) và nhấn "Áp dụng".<br>3. Frontend gửi lại yêu cầu `GET` với tham số thời gian mới.<br>4. Luồng hoạt động quay lại Bước 3 để tải và vẽ lại biểu đồ mới.<br><br>**6a. Xuất dữ liệu ra file (`Xuất báo cáo`):**<br>1. Tại bước 5 hoặc 6, Quản lý nhấn vào nút **"Xuất báo cáo"** và chọn định dạng **"Excel"** (hoặc PDF).<br>2. Frontend gửi yêu cầu Export xuống Backend, đính kèm khoảng thời gian đang hiển thị trên màn hình.<br>3. Backend truy vấn dữ liệu chi tiết và chuyển giao cho **Actor Pdf/Excel** (các thư viện như Apache POI / iText).<br>4. **Actor Pdf/Excel** xử lý và tạo ra một file vật lý chứa các bảng biểu thống kê.<br>5. Backend trả về luồng dữ liệu (File Stream) cho Frontend.<br>6. Trình duyệt của Quản lý tự động tải file `Bao_cao_doanh_thu.xlsx` xuống máy tính.<br>7. Luồng hoạt động tiếp tục tại Bước 6.<br><br>**4a. Không có dữ liệu kinh doanh (Empty State):**<br>1. Tại bước 3, Backend truy vấn nhưng nhà hàng không có bất kỳ đơn hàng nào trong khoảng thời gian được chọn.<br>2. Backend trả về các chỉ số bằng `0` và danh sách rỗng `[]`.<br>3. Frontend hiển thị giao diện trống với thông báo thân thiện: *"Không có dữ liệu giao dịch nào trong khoảng thời gian này."* và ẩn các biểu đồ.<br>4. Quản lý có thể chọn lại khoảng thời gian khác (Quay lại luồng 2a). |
| **Exception flows**<br>(Luồng ngoại lệ) | **2b. Lỗi khoảng thời gian quá lớn (Bypass Validation / Performance Protection):**<br>1. Tại bước 2 (của luồng 2a), Quản lý cố tình chọn khoảng thời gian quá dài hoặc dùng tool can thiệp API truyền `start_date` > `end_date`.<br>2. Backend kiểm tra logic và phát hiện khoảng thời gian không hợp lệ hoặc vượt quá giới hạn truy vấn cho phép để chống treo DB.<br>3. Backend chặn truy vấn, trả về mã lỗi `400 Bad Request`.<br>4. Frontend hiển thị cảnh báo: *"Khoảng thời gian không hợp lệ hoặc vượt quá 12 tháng. Vui lòng chọn lại để đảm bảo tốc độ tải."*<br>5. Use case kết thúc thất bại.<br><br>**3a. Lỗi Quá thời gian truy vấn (Database Timeout):**<br>1. Tại bước 3, do lượng dữ liệu của nhà hàng quá lớn, câu lệnh `GROUP BY` chạy quá lâu vượt mức cho phép (> 30 giây).<br>2. CSDL tự động ngắt kết nối (Timeout) để bảo vệ tài nguyên máy chủ.<br>3. Backend trả về mã lỗi `504 Gateway Timeout` hoặc `500 Internal Server Error`.<br>4. Frontend hiển thị thông báo: *"Hệ thống đang xử lý lượng dữ liệu lớn. Vui lòng thử lại sau hoặc thu hẹp khoảng thời gian lọc."*<br><br>**4b (Luồng 6a). Lỗi tạo file báo cáo (Actor Pdf/Excel Crash / Out of Memory):**<br>1. Tại bước 4 của luồng 6a, khi **Actor Pdf/Excel** đang cố gắng ghi 100.000 dòng dữ liệu vào file Excel, máy chủ bị tràn RAM (Out of Memory) hoặc thư viện bị crash.<br>2. Backend bắt lỗi (catch exception) và trả về mã `500 Internal Server Error`.<br>3. Frontend hiển thị thông báo: *"Đã xảy ra lỗi trong quá trình tạo file báo cáo. Vui lòng liên hệ kỹ thuật hoặc thử lại sau."* |

---
*Wireframe tương ứng:*
![Wireframe Báo cáo doanh thu](./screenshots/wireframe-rest-report.png)